# Quick Plan: Improve First Action Delta Diagnostics

Date: 2026-05-11
Status: Complete

## Goal

Make first-frame teleoperation safety failures identify the exact joint and values causing the rejection.

## Scope

- Update first-action delta validation to report the joint name, leader value, follower startup value, and delta.
- Add a regression assertion for the joint name in the raised error.

## Verification

- `python3 -m unittest tests.test_tcp_teleop -v`
- `python3 scripts/run_server.py --config configs/teleop/remote_so101_tcp.yaml --dry-run`
- `git diff --check`
