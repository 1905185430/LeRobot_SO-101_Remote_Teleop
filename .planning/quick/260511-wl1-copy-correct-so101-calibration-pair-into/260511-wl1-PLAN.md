# Quick Plan: Copy Correct SO101 Calibration Pair Into Repository

Date: 2026-05-11
Status: Complete

## Goal

Use the correct paired SO-101 calibration files from the project repository instead of relying on cache lookup.

## Scope

- Copy the matched cache pair into `calibrations/`:
  - `follower_arm.json`
  - `leader_arm.json`
- Update `configs/teleop/remote_so101_tcp.yaml` to use those project-local calibration directories.
- Keep IDs as `follower_arm` and `leader_arm`.
- Update tests to expect project-local calibration directories.

## Verification

- `cmp` project follower calibration against cache follower calibration.
- `cmp` project leader calibration against cache leader calibration.
- `python3 scripts/run_server.py --config configs/teleop/remote_so101_tcp.yaml --dry-run`
- `python3 scripts/run_client.py --config configs/teleop/remote_so101_tcp.yaml --dry-run`
- `python3 -m unittest tests.test_tcp_teleop tests.test_config_loader -v`
- `git diff --check`
