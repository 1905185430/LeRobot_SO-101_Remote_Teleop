# Quick Plan: Print TCP Teleop Leader Actions

Date: 2026-05-11
Status: Complete

## Goal

Show the joint/action values read from the teleoperation leader in the terminal before they are sent over TCP.

## Scope

- Add config flags under `logging` to enable leader action printing and control print interval.
- Print formatted leader action values from the TCP teleop leader loop.
- Enable the option in the local StarAI TCP teleop config.
- Cover config parsing and print behavior with unit tests.

## Verification

- `python3 -m unittest tests.test_tcp_teleop -v`
- `python3 -m unittest tests.test_config_loader -v`
- `python3 scripts/run_client.py --config configs/local_teleop_starai_tcp.yaml --dry-run`
- `python3 -m unittest discover -s tests -v`
- `git diff --check`
