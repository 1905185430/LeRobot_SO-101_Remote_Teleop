---
quick_id: 260511-q1u
slug: add-lightweight-server-webui-state-and-d
type: quick
status: planned
requirements: []
must_haves:
  truths:
    - "Server WebUI is optional and never prevents runtime startup when Gradio is absent."
    - "Dashboard state can hold config, connection status, joint state, action, latency, and log events."
    - "Mock TCP server updates dashboard state from received observations/actions."
    - "Docs explain WebUI current scope and limitations."
  artifacts:
    - path: "so101_remote/webui.py"
      provides: "Lightweight dashboard state and optional Gradio launcher"
---

# Quick Task: Add lightweight server WebUI state and dashboard

## Scope

Add the first server-side WebUI boundary without making Gradio a hard dependency. This gives a concrete place to display images, joint values, actions, latency, and logs as real observation streams are added.

## Tasks

1. Add WebUI state module.
   - Files: `so101_remote/webui.py`
   - Verify: unit tests cover state updates and missing-Gradio behavior.

2. Wire runtime startup.
   - Files: `so101_remote/runtime.py`
   - Verify: mock TCP server updates state and real server can launch optional dashboard.

3. Update docs.
   - Files: `docs/PROJECT_CN.md`, `README.md`
   - Verify: document install/use boundary.

4. Run regression tests.
   - Verify: `python3 -m unittest discover -s tests -v` and `git diff --check`.
