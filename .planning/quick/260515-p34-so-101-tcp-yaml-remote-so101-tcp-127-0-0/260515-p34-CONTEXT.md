# Quick Task 260515-p34: SO-101 Local TCP Teleop Config - Context

**Gathered:** 2026-05-15
**Status:** Ready for execution

<domain>
## Task Boundary

Add a local SO-101 TCP teleoperation YAML config for running leader and follower from one machine through `127.0.0.1`.

</domain>

<decisions>
## Implementation Decisions

- Reuse the existing SO-101 calibration pair:
  - follower: `follower_arm` under `calibrations/robots/so_follower`
  - leader: `leader_arm` under `calibrations/teleoperators/so_leader`
- Keep SO-101 safety settings aligned with `remote_so101_tcp.yaml`.
- Use role-explicit scripts for verification.

</decisions>

<specifics>
## Specific Ideas

- New config path: `configs/teleop/local_so101_tcp.yaml`.
- Local endpoint: `127.0.0.1:9011`.
- Default ports assume follower on `/dev/ttyACM1` and leader on `/dev/ttyACM0`; operators should edit these if their local device order differs.

</specifics>
