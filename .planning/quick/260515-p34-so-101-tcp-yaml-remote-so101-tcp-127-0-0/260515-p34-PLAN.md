# Quick Task 260515-p34: SO-101 Local TCP Teleop Config - Plan

**Mode:** quick
**Created:** 2026-05-15
**Status:** Ready

## Goal

Add a local SO-101 TCP teleoperation YAML config that can be dry-run through both role-explicit teleop entrypoints.

## Task 1: Add Config

**Files**
- `configs/teleop/local_so101_tcp.yaml`

**Action**
- Copy the SO-101 remote teleop shape.
- Set `network.server_host` to `127.0.0.1`.
- Use a separate save directory and local experiment name.

**Verify**
- `python3 scripts/run_teleop_follower.py --config configs/teleop/local_so101_tcp.yaml --dry-run`
- `python3 scripts/run_teleop_leader.py --config configs/teleop/local_so101_tcp.yaml --dry-run`
