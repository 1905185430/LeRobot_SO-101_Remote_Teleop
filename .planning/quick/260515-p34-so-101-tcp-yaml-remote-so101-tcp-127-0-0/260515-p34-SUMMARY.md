---
status: complete
quick_id: 260515-p34
date: 2026-05-15
---

# Quick Task 260515-p34 Summary

## Goal

Add a local SO-101 TCP teleoperation YAML config for running leader and follower through localhost.

## Changes

- Added `configs/teleop/local_so101_tcp.yaml`.
- Used local TCP endpoint `127.0.0.1:9011`.
- Reused the validated SO-101 calibration ids and directories:
  - follower: `follower_arm`, `calibrations/robots/so_follower`
  - leader: `leader_arm`, `calibrations/teleoperators/so_leader`
- Added a config loader test for the new local SO-101 teleop config.
- Restored `configs/teleop/local_starai_tcp.yaml` to the documented safe `max_first_action_delta: 12.0` after detecting local drift.

## Verification

- `python3 scripts/run_teleop_follower.py --config configs/teleop/local_so101_tcp.yaml --dry-run` passed.
- `python3 scripts/run_teleop_leader.py --config configs/teleop/local_so101_tcp.yaml --dry-run` passed.
- `python3 -m unittest tests.test_config_loader tests.test_tcp_teleop -v` passed: 23 tests.
- `git diff --check` passed.

## Operator Notes

- Default local ports are:
  - follower: `/dev/ttyACM1`
  - leader: `/dev/ttyACM0`
- Edit those ports before hardware runs if your local device order differs.
