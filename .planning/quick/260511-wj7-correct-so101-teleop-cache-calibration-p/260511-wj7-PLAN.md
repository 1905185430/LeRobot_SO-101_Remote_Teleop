# Quick Plan: Correct SO101 Teleop Cache Calibration Pair

Date: 2026-05-11
Status: Complete

## Goal

Correct the SO-101 remote teleoperation config to use the paired cache calibration IDs instead of mismatched IDs.

## Evidence

Cache files found:

- `robots/so_follower/follower_arm.json`: 2026-05-07 18:29
- `teleoperators/so_leader/leader_arm.json`: 2026-05-07 18:30
- `robots/so_follower/my_awesome_follower_arm.json`: 2026-05-07 23:49
- `teleoperators/so_leader/so101_leader_arm.json`: 2026-05-11 22:30

The closest timestamp pair is `follower_arm` and `leader_arm`. The user's pasted follower calibration matches `follower_arm.json`.

## Scope

- Update `configs/teleop/remote_so101_tcp.yaml` to use `follower_arm` and `leader_arm`.
- Keep `calibration_dir: null` so each machine resolves from its own LeRobot cache.
- Update tests to expect the corrected IDs.

## Verification

- `python3 scripts/run_server.py --config configs/teleop/remote_so101_tcp.yaml --dry-run`
- `python3 scripts/run_client.py --config configs/teleop/remote_so101_tcp.yaml --dry-run`
- `python3 -m unittest tests.test_tcp_teleop tests.test_config_loader -v`
- `git diff --check`
