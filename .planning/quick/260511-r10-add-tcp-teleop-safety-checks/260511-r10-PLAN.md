---
quick_id: 260511-r10
slug: add-tcp-teleop-safety-checks
type: quick
status: planned
requirements: []
must_haves:
  truths:
    - "TCP teleop rejects non-finite action values."
    - "TCP teleop rejects out-of-range action values."
    - "TCP teleop can enforce action key equality with follower joints."
    - "TCP teleop blocks startup when first leader action is too far from follower pose."
    - "Safety thresholds are configurable through YAML."
  artifacts:
    - path: "so101_remote/config_schema.py"
      provides: "Safety config schema"
    - path: "so101_remote/teleop_tcp.py"
      provides: "Teleop safety checks"
---

# Quick Task: Add TCP teleop safety checks

## Scope

Add configurable safety checks before follower `send_action()` so obvious calibration, mapping, and numeric errors stop teleoperation instead of moving hardware.
