---
phase: 04-so-101-smolvla-runtime-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - so101_remote/server.py
  - tests/test_minimal_async_scripts.py
autonomous: true
requirements: [RUN-01, RUN-04, RUN-05]
must_haves:
  truths:
    - "Policy server startup still uses LeRobot official async inference serve(config)."
    - "policy_server.py remains a thin entrypoint through so101_remote.server."
    - "Server startup creates its own run directory and metadata with resolved settings."
    - "Server startup records diagnostic events and re-raises startup failures."
    - "Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-10, D-11, and D-12 for the server side."
  artifacts:
    - path: "so101_remote/server.py"
      provides: "Policy server orchestration, resolved settings, metadata, and reliability hooks"
      contains: "run_policy_server"
    - path: "tests/test_minimal_async_scripts.py"
      provides: "Server orchestration tests with fake LeRobot modules"
      contains: "test_policy_server_runtime_writes_metadata"
---

<objective>
Move the policy server runtime construction into `so101_remote.server` with lightweight orchestration, per-server run artifacts, resolved settings, and reliability event recording.

Purpose: The GPU/server side should remain a thin LeRobot async inference wrapper while producing reproducible startup metadata for real LAN experiments.
</objective>

<context>
@.planning/phases/04-so-101-smolvla-runtime-integration/04-CONTEXT.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-RESEARCH.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-VALIDATION.md
@so101_remote/server.py
@so101_remote/config.py
@so101_remote/recorder.py
@so101_remote/reliability.py
@tests/test_minimal_async_scripts.py
</context>

<threat_model>
- T4-01: Server orchestration bypasses LeRobot official transport. Mitigation: `run_policy_server()` must still call LeRobot `serve(config)` with `PolicyServerConfig`.
- T4-02: Server failures are hidden by artifact handling. Mitigation: record exception events and re-raise.
- T4-03: Metadata omits settings needed to reproduce the run. Mitigation: `server_settings()` must include `host`, `port`, and `endpoint`, and metadata must include `resolved_settings`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add server resolved settings and metadata helpers</name>
  <files>so101_remote/server.py, tests/test_minimal_async_scripts.py</files>
  <read_first>so101_remote/server.py, so101_remote/config.py, so101_remote/recorder.py, tests/test_minimal_async_scripts.py</read_first>
  <action>In `so101_remote/server.py`, add `server_settings() -> dict[str, object]` returning exactly `{"host": HOST, "port": PORT, "endpoint": f"{HOST}:{PORT}"}`. Add `build_server_metadata(run_dir: str | Path) -> dict[str, object]` that calls `build_run_metadata(role="policy-server", server=server_settings(), policy={"type": POLICY_TYPE, "pretrained_name_or_path": PRETRAINED_NAME_OR_PATH, "device": POLICY_DEVICE}, extra={"resolved_settings": server_settings(), "run_dir": str(run_dir)})`. Import `Path`, `build_run_metadata`, and policy constants from `so101_remote.config`. Add tests in `tests/test_minimal_async_scripts.py` verifying `policy_server.server_settings()` exposes `host`, `port`, `endpoint`, and `policy_server.build_server_metadata("/tmp/server-run")` returns role `policy-server`, server endpoint, policy type `smolvla`, and `extra["resolved_settings"]["port"] == policy_server.PORT`.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts -v</verify>
  <acceptance_criteria>
    - `so101_remote/server.py` contains `def server_settings`
    - `so101_remote/server.py` contains `def build_server_metadata`
    - `so101_remote/server.py` contains `role="policy-server"`
    - `tests/test_minimal_async_scripts.py` contains `test_policy_server_settings_are_resolved`
    - `tests/test_minimal_async_scripts.py` contains `test_policy_server_metadata_includes_resolved_settings`
  </acceptance_criteria>
  <done>Server settings can be printed and persisted without touching LeRobot APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Add server orchestration with run artifacts and reliability events</name>
  <files>so101_remote/server.py, tests/test_minimal_async_scripts.py</files>
  <read_first>so101_remote/server.py, so101_remote/recorder.py, so101_remote/reliability.py, tests/test_minimal_async_scripts.py</read_first>
  <action>Add `run_policy_server(root: str | Path | None = None) -> int` in `so101_remote/server.py`. It must create a run directory with `create_run_directory(root or DEFAULT_RUN_ROOT, role="policy-server")`, build metadata using `build_server_metadata(run_dir)`, open `JsonlMetricsRecorder(run_dir, metadata=metadata)`, print a startup line containing `Policy server settings:` and `server_settings()`, record a `MetricEvent(EVENT_RECOVERY, "policy server startup configured", details={"stage": STAGE_SERVER_STARTUP, "component": "policy_server"})`, build the server config, call LeRobot `serve(config)`, call `recorder.write_summary()` after `serve` returns, and return `0`. Wrap the body so any `Exception` records `record_exception_event(recorder, stage=STAGE_SERVER_STARTUP, component="policy_server", exc=exc)` when a recorder exists, then re-raises. Update `main()` to return `run_policy_server()`. Add tests that monkeypatch `so101_remote.server.create_run_directory` to a temporary path, call `policy_server.main()`, assert LeRobot `serve` is called once, and assert `metadata.json`, `events.jsonl`, and `summary.md` exist with role `policy-server` and event type `recovery`. Add a test where fake `serve` raises `RuntimeError("server boom")` and assert the exception is re-raised and `events.jsonl` contains `"event_type": "exception"` and `server boom`.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts -v</verify>
  <acceptance_criteria>
    - `so101_remote/server.py` contains `def run_policy_server`
    - `so101_remote/server.py` contains `JsonlMetricsRecorder`
    - `so101_remote/server.py` contains `record_exception_event`
    - `tests/test_minimal_async_scripts.py` contains `test_policy_server_runtime_writes_metadata`
    - `tests/test_minimal_async_scripts.py` contains `test_policy_server_runtime_records_exception_and_reraises`
  </acceptance_criteria>
  <done>Server startup produces reproducible artifacts and visible diagnostics while preserving LeRobot serve.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
- [ ] `rg "run_policy_server|server_settings|build_server_metadata" so101_remote/server.py` finds server runtime APIs
- [ ] `rg "policy-server|resolved_settings|server boom" tests/test_minimal_async_scripts.py` finds server artifact assertions
</verification>

<success_criteria>
- Server can still be started through `policy_server.py`.
- Server runtime remains based on LeRobot official `serve(config)`.
- Server run metadata identifies endpoint, policy settings, resolved constants, run directory, and git commit when available.
- Server startup failures are recorded as events and re-raised.
- CONTEXT decisions D-01 through D-04, D-06 through D-08, and D-10 through D-12 are represented for the server runtime.
</success_criteria>
