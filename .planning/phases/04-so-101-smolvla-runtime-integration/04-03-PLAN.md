---
phase: 04-so-101-smolvla-runtime-integration
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - README.md
  - docs/ENVIRONMENT.md
  - tests/test_minimal_async_scripts.py
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04, RUN-05, RELY-03]
must_haves:
  truths:
    - "README and environment docs describe real server/client run artifacts and resolved settings."
    - "Automated tests prove the server and client artifact/reliability paths without LeRobot installed."
    - "The 10-30 minute LAN experiment readiness check is documented as hardware validation, not unit-test proof."
    - "Phase 4 does not add YAML config, CLI override systems, or a custom transport."
    - "Covers CONTEXT decisions D-05, D-09, D-10, D-11, and D-12 in documentation and phase-level validation."
  artifacts:
    - path: "README.md"
      provides: "Operator-facing runtime artifact guidance"
      contains: "Real Runtime Artifacts"
    - path: "docs/ENVIRONMENT.md"
      provides: "LAN experiment readiness and artifact checks"
      contains: "10-30 minute LAN experiment"
---

<objective>
Connect the server/client runtime artifact work into operator documentation and full automated verification, including clear 10-30 minute LAN experiment readiness guidance.

Purpose: Phase 4 should leave the real LeRobot path ready to try on the target LAN while being honest about what unit tests can and cannot prove.
</objective>

<context>
@.planning/phases/04-so-101-smolvla-runtime-integration/04-CONTEXT.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-RESEARCH.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-VALIDATION.md
@README.md
@docs/ENVIRONMENT.md
@tests/test_minimal_async_scripts.py
</context>

<threat_model>
- T4-07: Documentation implies unit tests validate real SO-101 hardware. Mitigation: explicitly separate automated checks from hardware validation.
- T4-08: Operator cannot find run artifacts after real startup. Mitigation: README and environment guide must name `logs/experiments/`, `metadata.json`, `events.jsonl`, and `summary.md`.
- T4-09: Phase 4 accidentally expands into config redesign. Mitigation: docs must continue to state constants are the v1 configuration path.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document real runtime artifact behavior</name>
  <files>README.md, docs/ENVIRONMENT.md</files>
  <read_first>README.md, docs/ENVIRONMENT.md, .planning/phases/04-so-101-smolvla-runtime-integration/04-CONTEXT.md</read_first>
  <action>Update README with a `## Real Runtime Artifacts` section explaining that `python3 policy_server.py` creates a `policy-server` run directory and `python3 robot_client.py` creates a `robot-client` run directory under `logs/experiments/`. State that each run directory includes `metadata.json`, `events.jsonl`, and `summary.md`, and that metadata includes resolved constant settings. Update `docs/ENVIRONMENT.md` under Metrics Artifact Check or a nearby section to explain the same real runtime artifact behavior and to keep constants as the v1 configuration path.</action>
  <verify>rg "Real Runtime Artifacts|policy-server|robot-client|resolved constant" README.md docs/ENVIRONMENT.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Real Runtime Artifacts`
    - `README.md` contains `policy-server`
    - `README.md` contains `robot-client`
    - `docs/ENVIRONMENT.md` contains `resolved constant`
    - `docs/ENVIRONMENT.md` contains `logs/experiments`
  </acceptance_criteria>
  <done>Operators know where real server/client artifacts are created and what settings they contain.</done>
</task>

<task type="auto">
  <name>Task 2: Document 10-30 minute LAN readiness validation</name>
  <files>docs/ENVIRONMENT.md</files>
  <read_first>docs/ENVIRONMENT.md, .planning/REQUIREMENTS.md, .planning/phases/04-so-101-smolvla-runtime-integration/04-VALIDATION.md</read_first>
  <action>Add a `## 10-30 Minute LAN Experiment Readiness` section to `docs/ENVIRONMENT.md`. It must instruct the operator to run the policy server on the GPU/server machine, run the robot client on the robot-side machine, confirm both run directories exist, inspect `metadata.json`, `events.jsonl`, and `summary.md`, and record whether any application-level crashes occurred during a 10-30 minute LAN run. Explicitly state that this check is required for RELY-03 and cannot be proven by unit tests alone.</action>
  <verify>rg "10-30 Minute LAN Experiment Readiness|RELY-03|cannot be proven by unit tests" docs/ENVIRONMENT.md</verify>
  <acceptance_criteria>
    - `docs/ENVIRONMENT.md` contains `## 10-30 Minute LAN Experiment Readiness`
    - `docs/ENVIRONMENT.md` contains `RELY-03`
    - `docs/ENVIRONMENT.md` contains `cannot be proven by unit tests`
    - `docs/ENVIRONMENT.md` contains `application-level crashes`
  </acceptance_criteria>
  <done>Hardware validation scope for RELY-03 is explicit and actionable.</done>
</task>

<task type="auto">
  <name>Task 3: Run full automated verification and close documentation/test gaps</name>
  <files>tests/test_minimal_async_scripts.py, README.md, docs/ENVIRONMENT.md</files>
  <read_first>tests/test_minimal_async_scripts.py, README.md, docs/ENVIRONMENT.md, .planning/phases/04-so-101-smolvla-runtime-integration/04-VALIDATION.md</read_first>
  <action>Run the full Phase 4 verification set. If focused runtime tests fail because documentation or tests need small updates, fix those issues within `tests/test_minimal_async_scripts.py`, `README.md`, or `docs/ENVIRONMENT.md`. Do not change runtime code in this task unless a prior task left an obvious test-only mismatch. Verification commands are `python3 -m unittest tests.test_minimal_async_scripts -v`, `python3 -m unittest tests.test_recorder tests.test_reliability tests.test_minimal_async_scripts -v`, and `python3 -m unittest discover -s tests -v`.</action>
  <verify>python3 -m unittest discover -s tests -v</verify>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
    - `python3 -m unittest tests.test_recorder tests.test_reliability tests.test_minimal_async_scripts -v` exits 0
    - `python3 -m unittest discover -s tests -v` exits 0
    - `rg "run_policy_server|run_robot_client|server_settings|client_settings" so101_remote` finds all four symbols
    - `rg "policy-server|robot-client|resolved_settings" tests so101_remote` finds artifact assertions and metadata fields
  </acceptance_criteria>
  <done>Phase 4 has automated coverage for runtime artifact paths and clear documentation for remaining hardware validation.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
- [ ] `python3 -m unittest tests.test_recorder tests.test_reliability tests.test_minimal_async_scripts -v` exits 0
- [ ] `python3 -m unittest discover -s tests -v` exits 0
- [ ] `rg "Real Runtime Artifacts|10-30 Minute LAN Experiment Readiness|RELY-03" README.md docs/ENVIRONMENT.md` finds docs
- [ ] `rg "run_policy_server|run_robot_client|server_settings|client_settings" so101_remote` finds runtime helpers
</verification>

<success_criteria>
- Operators can start the real server and client through thin entrypoints.
- Runtime metadata and logs identify server, robot, model, endpoint, run directory, and resolved settings.
- Official LeRobot async inference remains the transport path.
- RELY-03 has a concrete 10-30 minute LAN validation procedure.
- CONTEXT decision D-05 is preserved by documenting that shared cross-machine run IDs are deferred and not required for Phase 4.
</success_criteria>
