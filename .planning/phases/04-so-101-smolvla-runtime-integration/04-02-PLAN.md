---
phase: 04-so-101-smolvla-runtime-integration
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - so101_remote/client.py
  - tests/test_minimal_async_scripts.py
autonomous: true
requirements: [RUN-02, RUN-03, RUN-04, RUN-05, RELY-03]
must_haves:
  truths:
    - "Robot client startup still uses LeRobot official RobotClient and RobotClientConfig."
    - "robot_client.py remains a thin entrypoint through so101_remote.client."
    - "Robot client exposes all required SO-101, camera, model, action chunking, aggregation, and debug settings."
    - "Client startup creates its own run directory and metadata with resolved settings."
    - "Client startup records diagnostic events and re-raises runtime failures where practical."
    - "Covers CONTEXT decisions D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09, D-10, D-11, and D-12 for the robot-client side."
  artifacts:
    - path: "so101_remote/client.py"
      provides: "Robot client orchestration, resolved settings, metadata, and reliability hooks"
      contains: "run_robot_client"
    - path: "tests/test_minimal_async_scripts.py"
      provides: "Client orchestration tests with fake LeRobot modules"
      contains: "test_robot_client_runtime_writes_metadata"
---

<objective>
Move the SO-101 robot client runtime construction into `so101_remote.client` with lightweight orchestration, per-client run artifacts, resolved settings, and reliability event recording.

Purpose: The robot-side computer should keep the official LeRobot client path and all operator constants while producing reproducible client-side startup metadata and diagnostics.
</objective>

<context>
@.planning/phases/04-so-101-smolvla-runtime-integration/04-CONTEXT.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-RESEARCH.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-VALIDATION.md
@so101_remote/client.py
@so101_remote/config.py
@so101_remote/recorder.py
@so101_remote/reliability.py
@tests/test_minimal_async_scripts.py
</context>

<threat_model>
- T4-04: Client orchestration drops required SO-101 or SmolVLA settings. Mitigation: `client_settings()` must include every operator-facing client constant.
- T4-05: Client runtime hides failures that should stop hardware operation. Mitigation: record exception events, then re-raise except for the existing `client.start() == False` exit-code path.
- T4-06: Retry logic masks action execution problems. Mitigation: do not retry `control_loop()` or robot action execution.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add client resolved settings and metadata helpers</name>
  <files>so101_remote/client.py, tests/test_minimal_async_scripts.py</files>
  <read_first>so101_remote/client.py, so101_remote/config.py, so101_remote/recorder.py, tests/test_minimal_async_scripts.py</read_first>
  <action>In `so101_remote/client.py`, add `client_settings() -> dict[str, object]` returning a plain dictionary with keys `server_address`, `robot_port`, `robot_id`, `cameras`, `task`, `policy_type`, `pretrained_name_or_path`, `policy_device`, `actions_per_chunk`, `chunk_size_threshold`, `aggregate_fn_name`, and `debug_visualize_queue_size`, with values copied from the current constants. Add `build_client_metadata(run_dir: str | Path) -> dict[str, object]` that calls `build_run_metadata(role="robot-client", server={"address": SERVER_ADDRESS}, robot={"id": ROBOT_ID, "port": ROBOT_PORT, "cameras": CAMERAS}, policy={"type": POLICY_TYPE, "pretrained_name_or_path": PRETRAINED_NAME_OR_PATH, "device": POLICY_DEVICE}, extra={"resolved_settings": client_settings(), "run_dir": str(run_dir)})`. Import `Path` and `build_run_metadata`. Add tests verifying `robot_client.client_settings()` includes all required keys and `robot_client.build_client_metadata("/tmp/client-run")` returns role `robot-client`, robot id, server address, policy type, and `extra["resolved_settings"]["actions_per_chunk"] == robot_client.ACTIONS_PER_CHUNK`.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts -v</verify>
  <acceptance_criteria>
    - `so101_remote/client.py` contains `def client_settings`
    - `so101_remote/client.py` contains `def build_client_metadata`
    - `so101_remote/client.py` contains `role="robot-client"`
    - `tests/test_minimal_async_scripts.py` contains `test_robot_client_settings_are_resolved`
    - `tests/test_minimal_async_scripts.py` contains `test_robot_client_metadata_includes_resolved_settings`
  </acceptance_criteria>
  <done>Client settings can be printed and persisted without touching LeRobot APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Add client orchestration with run artifacts and reliability events</name>
  <files>so101_remote/client.py, tests/test_minimal_async_scripts.py</files>
  <read_first>so101_remote/client.py, so101_remote/recorder.py, so101_remote/reliability.py, tests/test_minimal_async_scripts.py</read_first>
  <action>Add `run_robot_client(root: str | Path | None = None) -> int` in `so101_remote/client.py`. It must create a run directory with `create_run_directory(root or DEFAULT_RUN_ROOT, role="robot-client")`, build metadata using `build_client_metadata(run_dir)`, open `JsonlMetricsRecorder(run_dir, metadata=metadata)`, print a startup line containing `Robot client settings:` and `client_settings()`, build the LeRobot client config, construct `RobotClient(config)`, and start it. If `client.start()` returns `False`, record `MetricEvent(EVENT_EXCEPTION, "robot client failed to start", severity="error", details={"stage": STAGE_CLIENT_STARTUP, "component": "robot_client"})`, write summary, and return `1`. If start succeeds, record `MetricEvent(EVENT_RECOVERY, "robot client startup complete", details={"stage": STAGE_CLIENT_STARTUP, "component": "robot_client"})`, start the daemon `receive_actions` thread, run `client.control_loop(TASK)`, write summary when the control loop returns, and return `0`. On `KeyboardInterrupt`, preserve existing behavior: call `client.stop()`, join the receiver thread, optionally visualize queue size, write summary, and return `0`. On any other `Exception`, record `record_exception_event(recorder, stage=STAGE_CLIENT_STARTUP, component="robot_client", exc=exc)` and re-raise. Update `main()` to return `run_robot_client()`. Add tests that monkeypatch `so101_remote.client.create_run_directory` to a temporary path, call `robot_client.main()`, and assert metadata/events/summary exist with role `robot-client` and event type `recovery`. Add a fake client variant whose `control_loop()` raises `RuntimeError("client boom")`; assert the exception is re-raised and `events.jsonl` contains `"event_type": "exception"` and `client boom`.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts -v</verify>
  <acceptance_criteria>
    - `so101_remote/client.py` contains `def run_robot_client`
    - `so101_remote/client.py` contains `JsonlMetricsRecorder`
    - `so101_remote/client.py` contains `record_exception_event`
    - `tests/test_minimal_async_scripts.py` contains `test_robot_client_runtime_writes_metadata`
    - `tests/test_minimal_async_scripts.py` contains `test_robot_client_runtime_records_exception_and_reraises`
  </acceptance_criteria>
  <done>Robot client startup produces reproducible artifacts and visible diagnostics while preserving LeRobot RobotClient behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve client operator exports and startup semantics</name>
  <files>robot_client.py, tests/test_minimal_async_scripts.py</files>
  <read_first>robot_client.py, so101_remote/client.py, tests/test_minimal_async_scripts.py</read_first>
  <action>Verify and adjust only if needed: top-level `robot_client.py` must continue exporting `SERVER_ADDRESS`, `ROBOT_PORT`, `ROBOT_ID`, `CAMERAS`, `TASK`, `POLICY_TYPE`, `PRETRAINED_NAME_OR_PATH`, `POLICY_DEVICE`, `ACTIONS_PER_CHUNK`, `CHUNK_SIZE_THRESHOLD`, `AGGREGATE_FN_NAME`, `DEBUG_VISUALIZE_QUEUE_SIZE`, `build_camera_configs`, `build_robot_config`, `build_client_config`, and `main`. Extend tests to assert `robot_client.main()` still starts the fake client, runs `receive_actions`, and runs `control_loop(TASK)` through the new `run_robot_client()` path.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts -v</verify>
  <acceptance_criteria>
    - `robot_client.py` contains `build_client_config`
    - `robot_client.py` contains `DEBUG_VISUALIZE_QUEUE_SIZE`
    - `tests/test_minimal_async_scripts.py` contains `test_top_level_wrappers_export_expected_helpers`
    - `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
  </acceptance_criteria>
  <done>Existing operator imports and tests remain compatible with the new client orchestration.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
- [ ] `python3 -m unittest tests.test_recorder tests.test_reliability tests.test_minimal_async_scripts -v` exits 0
- [ ] `rg "run_robot_client|client_settings|build_client_metadata" so101_remote/client.py` finds client runtime APIs
- [ ] `rg "robot-client|resolved_settings|client boom" tests/test_minimal_async_scripts.py` finds client artifact assertions
</verification>

<success_criteria>
- Robot client can still be started through `robot_client.py`.
- Robot client runtime remains based on LeRobot official `RobotClient`.
- Client metadata identifies server, robot, model, endpoint, resolved constants, run directory, and git commit when available.
- Client startup/control-loop failures are recorded as events and re-raised where appropriate.
- CONTEXT decisions D-01 through D-04 and D-06 through D-12 are represented for the robot-client runtime.
</success_criteria>
