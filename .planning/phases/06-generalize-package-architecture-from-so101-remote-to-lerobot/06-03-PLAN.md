---
phase: 06-generalize-package-architecture-from-so101-remote-to-lerobot
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - lerobot_remote/runtime/__init__.py
  - lerobot_remote/runtime/common.py
  - lerobot_remote/runtime/debug_mock.py
  - lerobot_remote/runtime/dispatch.py
  - lerobot_remote/runtime/local_inference.py
  - lerobot_remote/runtime/remote_inference.py
  - lerobot_remote/runtime/remote_teleop.py
  - lerobot_remote/webui/__init__.py
  - lerobot_remote/webui/app.py
  - lerobot_remote/webui/state.py
  - lerobot_remote/client.py
  - lerobot_remote/server.py
  - lerobot_remote/dryrun.py
  - policy_server.py
  - robot_client.py
  - scripts/run_server.py
  - scripts/run_client.py
  - scripts/run_local.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Runtime dispatch and mode-specific runners live under `lerobot_remote/runtime/`."
    - "WebUI state and Gradio app helpers are split under `lerobot_remote/webui/`."
    - "Thin CLI commands and top-level async entrypoints keep the same user-facing commands."
    - "Covers CONTEXT decisions D-05, D-06, D-07, D-11, D-14, D-15, and D-16."
  artifacts:
    - path: "lerobot_remote/runtime/__init__.py"
      provides: "Stable runtime imports for scripts"
      contains: "run_configured_server"
    - path: "scripts/run_server.py"
      provides: "Config-driven server CLI"
      contains: "from lerobot_remote.config import"
---

<objective>
Split runtime orchestration and WebUI code into layered modules, then update user-facing entrypoints to import from `lerobot_remote`. The command surface stays unchanged while the internal package becomes general and layered.
</objective>

<context>
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-CONTEXT.md
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-RESEARCH.md
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-PATTERNS.md
@so101_remote/runtime.py
@so101_remote/webui.py
@so101_remote/client.py
@so101_remote/server.py
@so101_remote/dryrun.py
@policy_server.py
@robot_client.py
@scripts/run_server.py
@scripts/run_client.py
@scripts/run_local.py
</context>

<threat_model>
- T6-07: CLI commands change during internal refactor. Mitigation: do not rename scripts or CLI flags; update imports only.
- T6-08: Runtime split creates circular imports between dispatch and mode modules. Mitigation: put shared helpers in `runtime/common.py` and keep `dispatch.py` as a thin switchboard.
- T6-09: Optional Gradio import becomes mandatory. Mitigation: preserve lazy `import_module("gradio")` inside `launch_dashboard`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Split WebUI state and app helpers</name>
  <files>lerobot_remote/webui/__init__.py, lerobot_remote/webui/state.py, lerobot_remote/webui/app.py</files>
  <read_first>so101_remote/webui.py, tests/test_webui.py</read_first>
  <action>Split `so101_remote/webui.py` into `lerobot_remote/webui/state.py` and `lerobot_remote/webui/app.py`. Move `DashboardState` and `snapshot_json` to `state.py`. Move `launch_dashboard`, `render_dashboard_snapshot`, and `render_images_html` to `app.py`. In `app.py`, import `DashboardState` from `.state` and `PlatformConfig` from `..config.schema`. Preserve lazy `gradio` import through `import_module("gradio")`. Re-export `DashboardState`, `launch_dashboard`, `render_dashboard_snapshot`, `render_images_html`, and `snapshot_json` from `lerobot_remote/webui/__init__.py`.</action>
  <verify>python3 - <<'PY'\nfrom lerobot_remote.config import load_config\nfrom lerobot_remote.webui import DashboardState, render_dashboard_snapshot, render_images_html, snapshot_json\nstate = DashboardState.from_config(load_config("configs/debug/debug_mock_robot.yaml"), "test")\nassert "debug" in snapshot_json(state)\nassert render_images_html({}) == "<p>No images received.</p>"\nprint("webui imports ok")\nPY</verify>
  <acceptance_criteria>
    - `lerobot_remote/webui/state.py` contains `class DashboardState`
    - `lerobot_remote/webui/app.py` contains `def launch_dashboard`
    - `lerobot_remote/webui/app.py` contains `import_module("gradio")`
    - `python3 -c "from lerobot_remote.webui import DashboardState, launch_dashboard, snapshot_json"` exits 0
  </acceptance_criteria>
  <done>WebUI state and app code are split and remain optional.</done>
</task>

<task type="auto">
  <name>Task 2: Split runtime dispatch and mode runners</name>
  <files>lerobot_remote/runtime/__init__.py, lerobot_remote/runtime/common.py, lerobot_remote/runtime/debug_mock.py, lerobot_remote/runtime/dispatch.py, lerobot_remote/runtime/local_inference.py, lerobot_remote/runtime/remote_inference.py, lerobot_remote/runtime/remote_teleop.py</files>
  <read_first>so101_remote/runtime.py, lerobot_remote/config/schema.py, lerobot_remote/policies/lerobot_async.py, lerobot_remote/teleop/__init__.py, lerobot_remote/robots/factory.py, lerobot_remote/network/tcp_client.py, lerobot_remote/network/tcp_server.py, lerobot_remote/webui/__init__.py, tests/test_configured_runtime.py, tests/test_tcp_teleop.py</read_first>
  <action>Split `so101_remote/runtime.py` into runtime modules. Put `configured_runtime_summary`, `run_configured_server`, `run_configured_client`, and `run_configured_local` in `dispatch.py`. Put `_build_configured_metadata`, `_create_configured_run_dir`, `_copy_source_config`, `_maybe_launch_dashboard`, `_disconnect_best_effort`, and `_mock_joint_positions` in `common.py`. Put `run_lerobot_policy_server`, `run_lerobot_robot_client`, `_load_lerobot_serve`, and `_load_lerobot_robot_client_api` in `remote_inference.py`. Put `run_tcp_teleop_follower_server` and `run_tcp_teleop_leader_client` in `remote_teleop.py`. Put `run_mock_tcp_server`, `run_mock_tcp_client`, and `run_local_mock_loop` in `debug_mock.py`. Put the current local inference not-implemented error path in `local_inference.py` if it is not directly handled in `dispatch.py`. Re-export the public runtime entrypoints from `lerobot_remote/runtime/__init__.py`.</action>
  <verify>python3 - <<'PY'\nfrom lerobot_remote.config import load_config\nfrom lerobot_remote.runtime import configured_runtime_summary, run_configured_client, run_configured_local, run_configured_server\nsummary = configured_runtime_summary("server", load_config("configs/debug/debug_mock_robot.yaml"))\nassert summary["role"] == "server"\nprint("runtime imports ok")\nPY</verify>
  <acceptance_criteria>
    - `lerobot_remote/runtime/dispatch.py` contains `def run_configured_server`
    - `lerobot_remote/runtime/remote_inference.py` contains `def run_lerobot_policy_server`
    - `lerobot_remote/runtime/remote_teleop.py` contains `def run_tcp_teleop_follower_server`
    - `lerobot_remote/runtime/debug_mock.py` contains `def run_mock_tcp_server`
    - `lerobot_remote/runtime/__init__.py` contains `run_configured_client`
    - `python3 -c "from lerobot_remote.runtime import configured_runtime_summary, run_configured_server, run_configured_client, run_configured_local"` exits 0
  </acceptance_criteria>
  <done>Runtime orchestration is split without changing the CLI-facing dispatch API.</done>
</task>

<task type="auto">
  <name>Task 3: Move thin async runtime modules and update entrypoint imports</name>
  <files>lerobot_remote/client.py, lerobot_remote/server.py, lerobot_remote/dryrun.py, policy_server.py, robot_client.py, scripts/run_server.py, scripts/run_client.py, scripts/run_local.py</files>
  <read_first>so101_remote/client.py, so101_remote/server.py, so101_remote/dryrun.py, policy_server.py, robot_client.py, scripts/run_server.py, scripts/run_client.py, scripts/run_local.py, tests/test_minimal_async_scripts.py, tests/test_dryrun.py</read_first>
  <action>Move `so101_remote/client.py` to `lerobot_remote/client.py`, `so101_remote/server.py` to `lerobot_remote/server.py`, and `so101_remote/dryrun.py` to `lerobot_remote/dryrun.py`. Update their imports to use `lerobot_remote.config.defaults`, `lerobot_remote.recording.metrics`, `lerobot_remote.recording.recorder`, and `lerobot_remote.reliability`. Update top-level `policy_server.py` to import from `lerobot_remote.server`. Update top-level `robot_client.py` to import from `lerobot_remote.client`. Update `scripts/run_server.py`, `scripts/run_client.py`, and `scripts/run_local.py` to import `load_config` and `ConfigError` from `lerobot_remote.config`, and runtime entrypoints from `lerobot_remote.runtime`. Do not change CLI argument names or command behavior.</action>
  <verify>python3 - <<'PY'\nimport policy_server\nimport robot_client\nfrom lerobot_remote.client import client_settings\nfrom lerobot_remote.server import server_settings\nfrom lerobot_remote.dryrun import run_dry_run\nprint("entrypoint imports ok")\nPY</verify>
  <acceptance_criteria>
    - `policy_server.py` contains `from lerobot_remote.server import`
    - `robot_client.py` contains `from lerobot_remote.client import`
    - `scripts/run_server.py` contains `from lerobot_remote.config import ConfigError, load_config`
    - `scripts/run_client.py` contains `from lerobot_remote.runtime import configured_runtime_summary, run_configured_client`
    - `scripts/run_local.py` contains `from lerobot_remote.runtime import configured_runtime_summary, run_configured_local`
    - `python3 scripts/run_server.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
    - `python3 scripts/run_client.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
  </acceptance_criteria>
  <done>All user-facing Python entrypoints import from `lerobot_remote` while keeping commands stable.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -c "from lerobot_remote.webui import DashboardState, launch_dashboard, snapshot_json"` exits 0
- [ ] `python3 -c "from lerobot_remote.runtime import configured_runtime_summary, run_configured_server, run_configured_client, run_configured_local"` exits 0
- [ ] `python3 -c "import policy_server, robot_client"` exits 0
- [ ] `python3 scripts/run_server.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
- [ ] `python3 scripts/run_client.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
</verification>

<success_criteria>
- Runtime modules are split by mode and dispatch responsibility.
- WebUI optional import behavior is preserved.
- Thin entrypoint command surface remains unchanged.
- Debug mock dry-run entrypoint checks pass.
</success_criteria>
