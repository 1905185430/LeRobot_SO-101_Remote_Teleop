---
phase: 06-generalize-package-architecture-from-so101-remote-to-lerobot
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lerobot_remote/__init__.py
  - lerobot_remote/config/__init__.py
  - lerobot_remote/config/defaults.py
  - lerobot_remote/config/loader.py
  - lerobot_remote/config/schema.py
  - lerobot_remote/recording/__init__.py
  - lerobot_remote/recording/metrics.py
  - lerobot_remote/recording/recorder.py
  - lerobot_remote/network/__init__.py
  - lerobot_remote/network/protocol.py
  - lerobot_remote/network/tcp_client.py
  - lerobot_remote/network/tcp_server.py
  - lerobot_remote/adapters/__init__.py
  - lerobot_remote/adapters/lerobot_so101.py
  - lerobot_remote/adapters/policy.py
  - lerobot_remote/adapters/robot.py
  - lerobot_remote/reliability.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Creates `lerobot_remote/` as the new real implementation package."
    - "Moves foundational modules out of `so101_remote/` without adding a `so101_remote` compatibility shim."
    - "Preserves the dirty local `SERVER_ADDRESS = \"192.168.1.151:8080\"` value when moving defaults."
    - "Covers CONTEXT decisions D-01, D-02, D-03, D-07, D-12, and D-13."
  artifacts:
    - path: "lerobot_remote/config/defaults.py"
      provides: "Constant-based v1 settings formerly in so101_remote/config.py"
      contains: "SERVER_ADDRESS = \"192.168.1.151:8080\""
    - path: "lerobot_remote/config/__init__.py"
      provides: "Ergonomic config imports"
      contains: "load_config"
---

<objective>
Create the `lerobot_remote` package and move low-coupling foundation modules into their target layered locations: config, recording, network, adapters, and reliability. This plan intentionally avoids teleop/runtime splitting so later plans can build on a stable package base.
</objective>

<context>
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-CONTEXT.md
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-RESEARCH.md
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-PATTERNS.md
@so101_remote/config.py
@so101_remote/config_loader.py
@so101_remote/config_schema.py
@so101_remote/metrics.py
@so101_remote/recorder.py
@so101_remote/network/protocol.py
@so101_remote/network/tcp_client.py
@so101_remote/network/tcp_server.py
@so101_remote/adapters/robot.py
@so101_remote/adapters/policy.py
@so101_remote/adapters/lerobot_so101.py
@so101_remote/reliability.py
</context>

<threat_model>
- T6-01: User-local config changes are lost while moving files. Mitigation: read `so101_remote/config.py` before moving and verify `lerobot_remote/config/defaults.py` contains `SERVER_ADDRESS = "192.168.1.151:8080"`.
- T6-02: Moving modules introduces import failures before later plans run. Mitigation: update intra-package imports for moved foundation modules immediately and run focused import tests after this plan.
- T6-03: A compatibility shim is accidentally created despite the user's clean-break decision. Mitigation: do not create forwarding modules under `so101_remote/`; leave old package cleanup to the final plan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create lerobot_remote package skeleton</name>
  <files>lerobot_remote/__init__.py, lerobot_remote/config/__init__.py, lerobot_remote/recording/__init__.py, lerobot_remote/network/__init__.py, lerobot_remote/adapters/__init__.py</files>
  <read_first>so101_remote/__init__.py, so101_remote/network/__init__.py, so101_remote/adapters/__init__.py</read_first>
  <action>Create directories `lerobot_remote/`, `lerobot_remote/config/`, `lerobot_remote/recording/`, `lerobot_remote/network/`, and `lerobot_remote/adapters/`. Add `__init__.py` files. `lerobot_remote/__init__.py` should contain a short package docstring such as `"""LeRobot remote VLA inference and TCP teleoperation package."""`. Do not create any `so101_remote` forwarding or compatibility files.</action>
  <verify>test -f lerobot_remote/__init__.py && test -f lerobot_remote/config/__init__.py && test -f lerobot_remote/recording/__init__.py && test -f lerobot_remote/network/__init__.py && test -f lerobot_remote/adapters/__init__.py</verify>
  <acceptance_criteria>
    - `test -f lerobot_remote/__init__.py` exits 0
    - `test -f lerobot_remote/config/__init__.py` exits 0
    - `test -f lerobot_remote/recording/__init__.py` exits 0
    - `rg -n "so101_remote" lerobot_remote/__init__.py lerobot_remote/config/__init__.py lerobot_remote/recording/__init__.py` returns no matches
  </acceptance_criteria>
  <done>New package skeleton exists without a compatibility shim.</done>
</task>

<task type="auto">
  <name>Task 2: Move config modules and preserve defaults</name>
  <files>lerobot_remote/config/defaults.py, lerobot_remote/config/loader.py, lerobot_remote/config/schema.py, lerobot_remote/config/__init__.py</files>
  <read_first>so101_remote/config.py, so101_remote/config_loader.py, so101_remote/config_schema.py, tests/test_config_loader.py</read_first>
  <action>Move the actual current contents of `so101_remote/config.py` to `lerobot_remote/config/defaults.py`, `so101_remote/config_loader.py` to `lerobot_remote/config/loader.py`, and `so101_remote/config_schema.py` to `lerobot_remote/config/schema.py`. In `loader.py`, change `from .config_schema import ...` to `from .schema import ConfigError, PlatformConfig, platform_config_from_mapping`. In `lerobot_remote/config/__init__.py`, re-export `ConfigError`, `PlatformConfig`, `load_config`, `parse_simple_yaml`, and `platform_config_from_mapping`. Preserve `SERVER_ADDRESS = "192.168.1.151:8080"` exactly in `defaults.py`.</action>
  <verify>python3 - <<'PY'\nfrom lerobot_remote.config import ConfigError, PlatformConfig, load_config, parse_simple_yaml, platform_config_from_mapping\nfrom lerobot_remote.config.defaults import SERVER_ADDRESS\nassert SERVER_ADDRESS == "192.168.1.151:8080"\nconfig = load_config("configs/teleop/remote_so101_tcp.yaml")\nassert config.mode == "remote_teleoperation"\nprint("config imports ok")\nPY</verify>
  <acceptance_criteria>
    - `lerobot_remote/config/defaults.py` contains `SERVER_ADDRESS = "192.168.1.151:8080"`
    - `lerobot_remote/config/loader.py` contains `from .schema import ConfigError, PlatformConfig, platform_config_from_mapping`
    - `lerobot_remote/config/__init__.py` contains `load_config`
    - `python3 -c "from lerobot_remote.config import load_config; print(load_config('configs/teleop/remote_so101_tcp.yaml').mode)"` exits 0 and prints `remote_teleoperation`
  </acceptance_criteria>
  <done>Config layer imports and bundled configs load through `lerobot_remote.config`.</done>
</task>

<task type="auto">
  <name>Task 3: Move recording, network, adapter, and reliability modules</name>
  <files>lerobot_remote/recording/metrics.py, lerobot_remote/recording/recorder.py, lerobot_remote/network/protocol.py, lerobot_remote/network/tcp_client.py, lerobot_remote/network/tcp_server.py, lerobot_remote/adapters/robot.py, lerobot_remote/adapters/policy.py, lerobot_remote/adapters/lerobot_so101.py, lerobot_remote/reliability.py</files>
  <read_first>so101_remote/metrics.py, so101_remote/recorder.py, so101_remote/network/protocol.py, so101_remote/network/tcp_client.py, so101_remote/network/tcp_server.py, so101_remote/adapters/robot.py, so101_remote/adapters/policy.py, so101_remote/adapters/lerobot_so101.py, so101_remote/reliability.py, tests/test_metrics.py, tests/test_recorder.py, tests/test_tcp_network.py, tests/test_reliability.py, tests/test_adapters.py</read_first>
  <action>Move `metrics.py` to `lerobot_remote/recording/metrics.py`, `recorder.py` to `lerobot_remote/recording/recorder.py`, `reliability.py` to `lerobot_remote/reliability.py`, the `network/` modules to `lerobot_remote/network/`, and the `adapters/` modules to `lerobot_remote/adapters/`. Update relative imports after the move: `recorder.py` should import metrics from `.metrics`; modules outside `recording/` should later import from `lerobot_remote.recording.metrics` and `lerobot_remote.recording.recorder`. In `lerobot_remote/recording/__init__.py`, re-export `MetricEvent`, `MetricSample`, `JsonlMetricsRecorder`, `build_run_metadata`, and `create_run_directory`.</action>
  <verify>python3 - <<'PY'\nfrom lerobot_remote.recording.metrics import MetricEvent, MetricSample\nfrom lerobot_remote.recording.recorder import JsonlMetricsRecorder, build_run_metadata, create_run_directory\nfrom lerobot_remote.network.protocol import make_ack_message\nfrom lerobot_remote.reliability import record_exception_event\nfrom lerobot_remote.adapters import RobotAdapter, PolicyAdapter, SO101LeRobotAdapter\nprint("foundation imports ok")\nPY</verify>
  <acceptance_criteria>
    - `python3 -c "from lerobot_remote.recording.metrics import MetricEvent; print(MetricEvent.__name__)"` prints `MetricEvent`
    - `python3 -c "from lerobot_remote.network.protocol import MSG_ACTION; print(MSG_ACTION)"` prints `ACTION`
    - `python3 -c "from lerobot_remote.adapters import RobotAdapter, PolicyAdapter, SO101LeRobotAdapter"` exits 0
    - `python3 -m unittest tests.test_metrics tests.test_recorder tests.test_tcp_network tests.test_reliability tests.test_adapters -v` exits 0 after tests are updated in the final plan
  </acceptance_criteria>
  <done>Foundation modules live under `lerobot_remote` and import cleanly.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -c "from lerobot_remote.config import load_config; print(load_config('configs/teleop/remote_so101_tcp.yaml').mode)"` exits 0
- [ ] `python3 -c "from lerobot_remote.recording.metrics import MetricEvent; print(MetricEvent.__name__)"` exits 0
- [ ] `python3 -c "from lerobot_remote.network.protocol import MSG_ACTION; print(MSG_ACTION)"` exits 0
- [ ] `python3 -c "from lerobot_remote.adapters import RobotAdapter, PolicyAdapter, SO101LeRobotAdapter"` exits 0
- [ ] `rg -n "SERVER_ADDRESS = \"192.168.1.151:8080\"" lerobot_remote/config/defaults.py` finds the preserved user-local setting
</verification>

<success_criteria>
- `lerobot_remote` package exists.
- Config, recording, network, adapter, and reliability layers are in their target locations.
- User-local v1 constant settings are preserved.
- No `so101_remote` compatibility shim is introduced.
</success_criteria>
