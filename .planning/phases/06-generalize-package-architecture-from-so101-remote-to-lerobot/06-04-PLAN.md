---
phase: 06-generalize-package-architecture-from-so101-remote-to-lerobot
plan: "04"
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - tests/test_adapters.py
  - tests/test_config_loader.py
  - tests/test_configured_runtime.py
  - tests/test_dryrun.py
  - tests/test_lerobot_factory.py
  - tests/test_metrics.py
  - tests/test_minimal_async_scripts.py
  - tests/test_recorder.py
  - tests/test_reliability.py
  - tests/test_starai.py
  - tests/test_tcp_network.py
  - tests/test_tcp_teleop.py
  - tests/test_webui.py
  - README.md
  - docs/project/PROJECT_CN.md
  - docs/validation/VALIDATION.md
  - docs/reproduction/REPRODUCTION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Repository-owned runtime code, scripts, tests, README, and operator docs no longer import or instruct use of `so101_remote`."
    - "Old `so101_remote/` implementation package is removed, with no compatibility shim."
    - "Full unit tests and required dry-run commands pass."
    - "Documentation does not claim new hardware validation from this refactor."
    - "Covers CONTEXT decisions D-03, D-14, D-15, D-16, and D-17."
  artifacts:
    - path: "README.md"
      provides: "Operator-facing package import examples"
      contains: "lerobot_remote"
    - path: "tests/test_config_loader.py"
      provides: "Config loader import coverage through new package path"
      contains: "lerobot_remote.config"
---

<objective>
Update tests and documentation to the new `lerobot_remote` package, remove the old implementation package, and run the required validation suite plus dry-run commands.
</objective>

<context>
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-CONTEXT.md
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-RESEARCH.md
@.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-PATTERNS.md
@tests/test_config_loader.py
@tests/test_configured_runtime.py
@tests/test_tcp_teleop.py
@tests/test_starai.py
@README.md
@docs/project/PROJECT_CN.md
@docs/validation/VALIDATION.md
@docs/reproduction/REPRODUCTION.md
</context>

<threat_model>
- T6-10: Tests pass by removing assertions instead of preserving coverage. Mitigation: update imports and patch targets, but keep behavioral assertions for SO-101, StarAI, TCP safety, config parsing, and dry-run.
- T6-11: Stale `so101_remote` docs confuse users after the clean break. Mitigation: update README and operator docs to `lerobot_remote`; leave only historical planning references outside operator surfaces if needed.
- T6-12: Generated logs/runs are accidentally committed during validation. Mitigation: stage only source, tests, docs, and planning artifacts; do not stage `logs/`, `runs/`, `map.png`, or generated run output.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update tests to import lerobot_remote</name>
  <files>tests/test_adapters.py, tests/test_config_loader.py, tests/test_configured_runtime.py, tests/test_dryrun.py, tests/test_lerobot_factory.py, tests/test_metrics.py, tests/test_minimal_async_scripts.py, tests/test_recorder.py, tests/test_reliability.py, tests/test_starai.py, tests/test_tcp_network.py, tests/test_tcp_teleop.py, tests/test_webui.py</files>
  <read_first>tests/test_adapters.py, tests/test_config_loader.py, tests/test_configured_runtime.py, tests/test_dryrun.py, tests/test_lerobot_factory.py, tests/test_metrics.py, tests/test_minimal_async_scripts.py, tests/test_recorder.py, tests/test_reliability.py, tests/test_starai.py, tests/test_tcp_network.py, tests/test_tcp_teleop.py, tests/test_webui.py, lerobot_remote/teleop/__init__.py, lerobot_remote/runtime/__init__.py, lerobot_remote/robots/factory.py, lerobot_remote/policies/lerobot_async.py</read_first>
  <action>Update all test imports from `so101_remote` to `lerobot_remote` and from old flat modules to new layered modules. Examples: `so101_remote.config_loader` becomes `lerobot_remote.config.loader` or `lerobot_remote.config`; `so101_remote.config_schema` becomes `lerobot_remote.config.schema` or `lerobot_remote.config`; `so101_remote.metrics` becomes `lerobot_remote.recording.metrics`; `so101_remote.recorder` becomes `lerobot_remote.recording.recorder`; `so101_remote.lerobot_factory` becomes `lerobot_remote.policies.lerobot_async`; `so101_remote.starai` becomes `lerobot_remote.robots.starai`; `so101_remote.teleop_tcp` becomes `lerobot_remote.teleop` or specific teleop modules. Update mock patch targets such as `so101_remote.runtime.run_tcp_teleop_follower_server` to the new location that the code under test actually resolves, likely `lerobot_remote.runtime.dispatch.run_tcp_teleop_follower_server` or the re-exported `lerobot_remote.runtime.run_tcp_teleop_follower_server` depending on implementation. Do not delete behavioral assertions to make tests pass.</action>
  <verify>python3 -m unittest discover -s tests -v</verify>
  <acceptance_criteria>
    - `rg -n "from so101_remote|import so101_remote|so101_remote\\." tests` returns no matches
    - `python3 -m unittest tests.test_config_loader tests.test_metrics tests.test_recorder tests.test_tcp_network -v` exits 0
    - `python3 -m unittest tests.test_starai tests.test_tcp_teleop tests.test_lerobot_factory -v` exits 0
    - `python3 -m unittest tests.test_minimal_async_scripts tests.test_configured_runtime tests.test_webui tests.test_dryrun tests.test_reliability tests.test_adapters -v` exits 0
  </acceptance_criteria>
  <done>Tests use the new package path and preserve behavior coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Update operator docs and remove old package implementation</name>
  <files>README.md, docs/project/PROJECT_CN.md, docs/validation/VALIDATION.md, docs/reproduction/REPRODUCTION.md, so101_remote/</files>
  <read_first>README.md, docs/project/PROJECT_CN.md, docs/validation/VALIDATION.md, docs/reproduction/REPRODUCTION.md, .planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-CONTEXT.md, .planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-RESEARCH.md</read_first>
  <action>Update operator-facing docs to refer to `lerobot_remote` instead of `so101_remote` for package imports, dry-run snippets, config layer references, TCP protocol package references, and project structure diagrams. Keep config experiment names like `so101_remote_teleop_tcp` unchanged unless the docs explicitly describe Python package names. Remove the old `so101_remote/` implementation package after all source imports and tests point to `lerobot_remote`. Do not leave a forwarding compatibility shim. Do not stage generated `logs/`, `runs/`, `map.png`, or `__pycache__/` content.</action>
  <verify>rg -n "from so101_remote|import so101_remote|so101_remote\\." README.md docs scripts tests policy_server.py robot_client.py lerobot_remote || true</verify>
  <acceptance_criteria>
    - `README.md` contains `lerobot_remote.dryrun`
    - `docs/project/PROJECT_CN.md` contains `lerobot_remote/`
    - `docs/validation/VALIDATION.md` contains `lerobot_remote/`
    - `test ! -d so101_remote` exits 0
    - `rg -n "from so101_remote|import so101_remote|so101_remote\\." README.md docs scripts tests policy_server.py robot_client.py lerobot_remote` returns no matches
  </acceptance_criteria>
  <done>Operator-facing docs and repository code no longer reference the old implementation package.</done>
</task>

<task type="auto">
  <name>Task 3: Run required validation suite and dry-runs</name>
  <files>tests/, scripts/run_server.py, scripts/run_client.py, scripts/run_local.py, README.md, docs/validation/VALIDATION.md</files>
  <read_first>.planning/phases/06-generalize-package-architecture-from-so101-remote-to-lerobot/06-CONTEXT.md, tests/test_config_loader.py, tests/test_tcp_teleop.py, tests/test_starai.py, scripts/run_server.py, scripts/run_client.py, configs/teleop/remote_so101_tcp.yaml, configs/teleop/local_starai_tcp.yaml, configs/debug/debug_mock_robot.yaml</read_first>
  <action>Run the required Phase 6 validation commands exactly: `python3 -m unittest discover -s tests -v`; `python3 scripts/run_server.py --config configs/teleop/remote_so101_tcp.yaml --dry-run`; `python3 scripts/run_client.py --config configs/teleop/remote_so101_tcp.yaml --dry-run`; `python3 scripts/run_server.py --config configs/teleop/local_starai_tcp.yaml --dry-run`; `python3 scripts/run_client.py --config configs/teleop/local_starai_tcp.yaml --dry-run`; `python3 scripts/run_server.py --config configs/debug/debug_mock_robot.yaml --dry-run`; `python3 scripts/run_client.py --config configs/debug/debug_mock_robot.yaml --dry-run`; `git diff --check`. If dry-runs create run artifacts, leave them untracked and do not commit them. If documentation is updated with validation results, explicitly say these are automated dry-run checks, not fresh hardware validation.</action>
  <verify>python3 -m unittest discover -s tests -v && python3 scripts/run_server.py --config configs/teleop/remote_so101_tcp.yaml --dry-run && python3 scripts/run_client.py --config configs/teleop/remote_so101_tcp.yaml --dry-run && python3 scripts/run_server.py --config configs/teleop/local_starai_tcp.yaml --dry-run && python3 scripts/run_client.py --config configs/teleop/local_starai_tcp.yaml --dry-run && python3 scripts/run_server.py --config configs/debug/debug_mock_robot.yaml --dry-run && python3 scripts/run_client.py --config configs/debug/debug_mock_robot.yaml --dry-run && git diff --check</verify>
  <acceptance_criteria>
    - `python3 -m unittest discover -s tests -v` exits 0
    - `python3 scripts/run_server.py --config configs/teleop/remote_so101_tcp.yaml --dry-run` exits 0
    - `python3 scripts/run_client.py --config configs/teleop/remote_so101_tcp.yaml --dry-run` exits 0
    - `python3 scripts/run_server.py --config configs/teleop/local_starai_tcp.yaml --dry-run` exits 0
    - `python3 scripts/run_client.py --config configs/teleop/local_starai_tcp.yaml --dry-run` exits 0
    - `python3 scripts/run_server.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
    - `python3 scripts/run_client.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
    - `git diff --check` exits 0
  </acceptance_criteria>
  <done>Phase 6 automated verification passes and does not claim hardware reruns.</done>
</task>

</tasks>

<verification>
- [ ] `rg -n "from so101_remote|import so101_remote|so101_remote\\." README.md docs scripts tests policy_server.py robot_client.py lerobot_remote` returns no matches
- [ ] `test ! -d so101_remote` exits 0
- [ ] `python3 -m unittest discover -s tests -v` exits 0
- [ ] `python3 scripts/run_server.py --config configs/teleop/remote_so101_tcp.yaml --dry-run` exits 0
- [ ] `python3 scripts/run_client.py --config configs/teleop/remote_so101_tcp.yaml --dry-run` exits 0
- [ ] `python3 scripts/run_server.py --config configs/teleop/local_starai_tcp.yaml --dry-run` exits 0
- [ ] `python3 scripts/run_client.py --config configs/teleop/local_starai_tcp.yaml --dry-run` exits 0
- [ ] `python3 scripts/run_server.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
- [ ] `python3 scripts/run_client.py --config configs/debug/debug_mock_robot.yaml --dry-run` exits 0
- [ ] `git diff --check` exits 0
</verification>

<success_criteria>
- Clean-break package rename is complete.
- No repository-owned runtime/test/operator surface imports `so101_remote`.
- Old implementation package is removed.
- Unit tests and required dry-runs pass.
- Docs reflect the new package name without over-claiming hardware validation.
</success_criteria>
