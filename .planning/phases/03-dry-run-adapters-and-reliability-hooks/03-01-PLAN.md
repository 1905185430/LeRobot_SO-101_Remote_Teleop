---
phase: 03-dry-run-adapters-and-reliability-hooks
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - so101_remote/adapters/robot.py
  - so101_remote/adapters/policy.py
  - so101_remote/adapters/lerobot_so101.py
  - so101_remote/adapters/__init__.py
  - tests/test_adapters.py
autonomous: true
requirements: [ADPT-01, ADPT-02, ADPT-03, ADPT-04, ADPT-05]
user_setup: []
must_haves:
  truths:
    - "Adapter modules import without LeRobot installed."
    - "Robot and policy adapter protocols define the small surface dry-run and runtime orchestration can depend on."
    - "SO-101 and SmolVLA have concrete lightweight adapter classes, while PI-series policies and other robot arms have placeholders without a plugin registry."
  artifacts:
    - path: "so101_remote/adapters/robot.py"
      provides: "Robot adapter protocol and placeholder robot adapter"
      contains: "RobotAdapter"
    - path: "so101_remote/adapters/policy.py"
      provides: "Policy adapter protocol and placeholder PI policy adapter"
      contains: "PolicyAdapter"
    - path: "so101_remote/adapters/lerobot_so101.py"
      provides: "SO-101 and SmolVLA concrete adapter classes"
      contains: "SO101LeRobotAdapter"
    - path: "tests/test_adapters.py"
      provides: "Adapter import and behavior tests"
      contains: "AdapterTests"
---

<objective>
Define minimal robot and policy adapter boundaries plus concrete SO-101/SmolVLA locations.

Purpose: Keep v1 lightweight while giving dry-run, later LeRobot runtime integration, PI-series policies, and other robot arms clear extension points.
Output: Import-safe adapter protocols/classes and `tests/test_adapters.py`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dry-run-adapters-and-reliability-hooks/03-VALIDATION.md
@.planning/phases/02-metrics-and-run-artifacts/02-01-SUMMARY.md
@.planning/phases/02-metrics-and-run-artifacts/02-02-SUMMARY.md
@.planning/phases/02-metrics-and-run-artifacts/02-03-SUMMARY.md

Relevant source files:
@so101_remote/adapters/robot.py
@so101_remote/adapters/policy.py
@so101_remote/adapters/lerobot_so101.py
@so101_remote/adapters/__init__.py
</context>

<threat_model>
- T3-01: Adapter modules import LeRobot at module import time and break local tests. Mitigation: use standard-library protocols/dataclasses only.
- T3-02: Adapter layer becomes a heavy plugin registry. Mitigation: define explicit classes and placeholders only; no dynamic discovery or registry.
- T3-03: Placeholder support is mistaken for implemented PI-series or second robot hardware. Mitigation: placeholder adapters raise `NotImplementedError` with explicit messages.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Expand robot adapter boundary</name>
  <files>so101_remote/adapters/robot.py</files>
  <read_first>so101_remote/adapters/robot.py, .planning/phases/03-dry-run-adapters-and-reliability-hooks/03-VALIDATION.md</read_first>
  <action>Replace the placeholder `RobotAdapter` with an import-safe protocol that includes attributes `robot_id: str` and methods `connect() -> None`, `disconnect() -> None`, `read_observation() -> dict[str, object]`, and `apply_action(action: object) -> None`. Add `UnsupportedRobotAdapter` dataclass with field `robot_id: str = "unsupported"`; every method must raise `NotImplementedError` with a message containing `No robot backend is implemented for`. Do not import LeRobot or hardware packages.</action>
  <verify>python3 -m unittest tests.test_adapters -v</verify>
  <acceptance_criteria>
    - `so101_remote/adapters/robot.py` contains `def connect`
    - `so101_remote/adapters/robot.py` contains `def read_observation`
    - `so101_remote/adapters/robot.py` contains `def apply_action`
    - `so101_remote/adapters/robot.py` contains `class UnsupportedRobotAdapter`
  </acceptance_criteria>
  <done>Robot runtime orchestration has a small hardware-independent boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Expand policy adapter boundary</name>
  <files>so101_remote/adapters/policy.py</files>
  <read_first>so101_remote/adapters/policy.py</read_first>
  <action>Replace the placeholder `PolicyAdapter` with an import-safe protocol that includes attribute `policy_type: str` and methods `load() -> None` and `infer_action(observation: dict[str, object]) -> object`. Add `UnsupportedPolicyAdapter` dataclass with field `policy_type: str = "unsupported"`; its methods must raise `NotImplementedError` with a message containing `No policy backend is implemented for`. Add `PISeriesPolicyPlaceholder` dataclass with field `policy_type: str = "pi-series"` that subclasses or wraps the unsupported behavior and raises a message containing `PI-series policy support is a placeholder`.</action>
  <verify>python3 -m unittest tests.test_adapters -v</verify>
  <acceptance_criteria>
    - `so101_remote/adapters/policy.py` contains `def load`
    - `so101_remote/adapters/policy.py` contains `def infer_action`
    - `so101_remote/adapters/policy.py` contains `class UnsupportedPolicyAdapter`
    - `so101_remote/adapters/policy.py` contains `class PISeriesPolicyPlaceholder`
  </acceptance_criteria>
  <done>Policy runtime orchestration has a small model-independent boundary and PI-series placeholder.</done>
</task>

<task type="auto">
  <name>Task 3: Implement concrete SO-101 and SmolVLA adapter classes and tests</name>
  <files>so101_remote/adapters/lerobot_so101.py, so101_remote/adapters/__init__.py, tests/test_adapters.py</files>
  <read_first>so101_remote/adapters/lerobot_so101.py, so101_remote/adapters/__init__.py, so101_remote/config.py, tests/test_metrics.py</read_first>
  <action>Update `SO101LeRobotAdapter` as a dataclass with fields `robot_id: str`, `port: str`, and `camera_names: tuple[str, ...]`; add `describe() -> dict[str, object]` returning keys `robot_id`, `port`, `camera_names`, and `backend` with value `"lerobot-so101"`. Keep `connect`, `disconnect`, `read_observation`, and `apply_action` methods raising `NotImplementedError` containing `LeRobot SO-101 runtime wiring happens in Phase 4`. Update `SmolVLAPolicyAdapter` as a dataclass with fields `policy_type: str = "smolvla"`, `pretrained_name_or_path: str = ""`, and `device: str = "cuda"`; add `describe() -> dict[str, object]` returning keys `policy_type`, `pretrained_name_or_path`, `device`, and `backend` with value `"lerobot-smolvla"`. Keep `load` and `infer_action` raising `NotImplementedError` containing `SmolVLA runtime wiring happens in Phase 4`. Export all adapter classes from `so101_remote/adapters/__init__.py`. Create `tests/test_adapters.py` verifying all adapter modules import, `describe()` outputs exact backend values, unsupported placeholders raise the expected messages, and no `lerobot` modules are imported during adapter imports.</action>
  <verify>python3 -m unittest tests.test_adapters -v</verify>
  <acceptance_criteria>
    - `so101_remote/adapters/lerobot_so101.py` contains `backend`
    - `so101_remote/adapters/lerobot_so101.py` contains `LeRobot SO-101 runtime wiring happens in Phase 4`
    - `so101_remote/adapters/lerobot_so101.py` contains `SmolVLA runtime wiring happens in Phase 4`
    - `so101_remote/adapters/__init__.py` contains `PISeriesPolicyPlaceholder`
    - `tests/test_adapters.py` contains `class AdapterTests`
    - `python3 -m unittest tests.test_adapters -v` exits 0
  </acceptance_criteria>
  <done>SO-101/SmolVLA have concrete lightweight adapter locations and future placeholders remain explicit.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_adapters -v` exits 0
- [ ] `python3 -c "import so101_remote.adapters.robot; import so101_remote.adapters.policy; import so101_remote.adapters.lerobot_so101"` exits 0
- [ ] `rg "PISeriesPolicyPlaceholder|UnsupportedRobotAdapter|SO101LeRobotAdapter|SmolVLAPolicyAdapter" so101_remote/adapters` finds all adapter classes
</verification>

<success_criteria>
- All tasks completed
- Adapter boundaries are import-safe and standard-library only
- SO-101/SmolVLA concrete locations exist without real runtime wiring
- PI-series and other robot placeholders are explicit and non-claiming
</success_criteria>

<output>
After completion, create `.planning/phases/03-dry-run-adapters-and-reliability-hooks/03-01-SUMMARY.md`
</output>
