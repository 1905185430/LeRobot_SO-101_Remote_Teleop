---
phase: 03-dry-run-adapters-and-reliability-hooks
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - so101_remote/dryrun.py
  - tests/test_dryrun.py
  - README.md
autonomous: true
requirements: [DRY-01, DRY-02, DRY-03, RELY-01]
user_setup: []
must_haves:
  truths:
    - "Developer can run dry-run code on one machine without SO-101 hardware or LeRobot installed."
    - "Dry-run creates a real run directory with metadata, metric files, events, and summary."
    - "Dry-run output explicitly does not validate real hardware, camera, model loading, or physical control."
  artifacts:
    - path: "so101_remote/dryrun.py"
      provides: "Fake robot/policy dry-run orchestration"
      contains: "run_dry_run"
    - path: "tests/test_dryrun.py"
      provides: "Dry-run artifact tests"
      contains: "DryRunTests"
    - path: "README.md"
      provides: "Dry-run command/API note"
      contains: "Dry Run"
---

<objective>
Add dry-run fake robot/policy execution that exercises metrics and run artifacts without hardware.

Purpose: Let developers validate the runtime plumbing and experiment artifact layout on one machine before SO-101 or SmolVLA runtime integration.
Output: `so101_remote.dryrun` implementation, tests, and concise README guidance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dry-run-adapters-and-reliability-hooks/03-VALIDATION.md
@.planning/phases/03-dry-run-adapters-and-reliability-hooks/03-01-PLAN.md
@.planning/phases/02-metrics-and-run-artifacts/02-01-SUMMARY.md
@.planning/phases/02-metrics-and-run-artifacts/02-02-SUMMARY.md
@.planning/phases/02-metrics-and-run-artifacts/02-03-SUMMARY.md

Relevant source files:
@so101_remote/dryrun.py
@so101_remote/metrics.py
@so101_remote/recorder.py
@so101_remote/adapters/robot.py
@so101_remote/adapters/policy.py
@README.md
</context>

<threat_model>
- T3-04: Dry-run accidentally claims real hardware or model validation. Mitigation: include `validates_hardware: false` metadata and explicit summary/event text.
- T3-05: Dry-run bypasses Phase 2 artifact APIs. Mitigation: use `create_run_directory`, `build_run_metadata`, `JsonlMetricsRecorder`, and `write_summary()`.
- T3-06: Dry-run depends on wall-clock sleeps and becomes flaky. Mitigation: simulate deterministic timestamps and values by default.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement fake robot and policy dry-run classes</name>
  <files>so101_remote/dryrun.py</files>
  <read_first>so101_remote/dryrun.py, so101_remote/adapters/robot.py, so101_remote/adapters/policy.py, so101_remote/metrics.py</read_first>
  <action>Replace the dry-run placeholder with standard-library code. Add `FakeRobotAdapter` dataclass with `robot_id: str = "dryrun-so101"` and methods `connect()`, `disconnect()`, `read_observation() -> dict[str, object]`, and `apply_action(action: object) -> None`. It should track `connected: bool`, `observations_read: int`, and `actions_applied: int`; `read_observation()` returns keys `sequence`, `timestamp`, and `source` with source `"dry-run"`. Add `FakePolicyAdapter` dataclass with `policy_type: str = "dryrun-policy"` and methods `load()` and `infer_action(observation: dict[str, object]) -> dict[str, object]`; it should track `loaded: bool` and return action keys `sequence` and `command` with command `"hold-position"`.</action>
  <verify>python3 -m unittest tests.test_dryrun -v</verify>
  <acceptance_criteria>
    - `so101_remote/dryrun.py` contains `class FakeRobotAdapter`
    - `so101_remote/dryrun.py` contains `class FakePolicyAdapter`
    - `so101_remote/dryrun.py` contains `hold-position`
    - `so101_remote/dryrun.py` contains `dry-run`
  </acceptance_criteria>
  <done>Dry-run has fake adapters that do not touch hardware or models.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dry-run orchestration and artifact recording</name>
  <files>so101_remote/dryrun.py, tests/test_dryrun.py</files>
  <read_first>so101_remote/dryrun.py, so101_remote/metrics.py, so101_remote/recorder.py, tests/test_recorder.py</read_first>
  <action>Add `run_dry_run(root: str | Path | None = None, iterations: int = 5) -> Path`. It must create a run directory under `root` when provided or under `logs/experiments` by default, build metadata with role `"dry-run"`, robot id `"dryrun-so101"`, policy type `"dryrun-policy"`, and extra keys `validates_hardware: False`, `validates_model: False`, and `purpose: "metrics plumbing only"`. Use `JsonlMetricsRecorder` to record for each iteration: one `loop_interval_ms` sample, one `latency_ms` sample, one `queue_size` sample, and a startup or recovery event. Call `write_summary()` before returning the run directory. Create `tests/test_dryrun.py` with `unittest` and `TemporaryDirectory`; assert the returned run directory contains `metadata.json`, `metrics.jsonl`, `events.jsonl`, `metrics.csv`, and `summary.md`; assert metadata has `role` `"dry-run"` and `validates_hardware` false; assert `metrics.jsonl` contains `loop_interval_ms`, `latency_ms`, and `queue_size`; assert `summary.md` contains `# Run Summary`.</action>
  <verify>python3 -m unittest tests.test_dryrun -v</verify>
  <acceptance_criteria>
    - `so101_remote/dryrun.py` contains `def run_dry_run`
    - `so101_remote/dryrun.py` contains `validates_hardware`
    - `tests/test_dryrun.py` contains `class DryRunTests`
    - `tests/test_dryrun.py` contains `test_run_dry_run_writes_artifact_set`
    - `python3 -m unittest tests.test_dryrun -v` exits 0
  </acceptance_criteria>
  <done>Dry-run writes realistic Phase 2 artifacts without hardware.</done>
</task>

<task type="auto">
  <name>Task 3: Document dry-run usage without overclaiming validation</name>
  <files>README.md</files>
  <read_first>README.md, docs/ENVIRONMENT.md, so101_remote/dryrun.py</read_first>
  <action>Add a concise `## Dry Run` section to `README.md`. Include a Python command that runs `from so101_remote.dryrun import run_dry_run; print(run_dry_run())`. State that dry-run validates code path, metrics plumbing, and artifact generation only. State exactly that it does not validate real SO-101 hardware, camera frames, real SmolVLA loading, inference quality, or physical safety behavior.</action>
  <verify>rg "## Dry Run|run_dry_run|does not validate real SO-101 hardware|metrics plumbing" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Dry Run`
    - `README.md` contains `run_dry_run`
    - `README.md` contains `does not validate real SO-101 hardware`
    - `README.md` contains `metrics plumbing`
  </acceptance_criteria>
  <done>Dry-run is discoverable and does not overclaim hardware validation.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_dryrun -v` exits 0
- [ ] `python3 -m unittest tests.test_metrics tests.test_recorder tests.test_dryrun -v` exits 0
- [ ] `python3 -c "from so101_remote.dryrun import run_dry_run; from tempfile import TemporaryDirectory; d=TemporaryDirectory(); print(run_dry_run(d.name).name)"` exits 0
- [ ] `rg "## Dry Run|run_dry_run|does not validate real SO-101 hardware" README.md` finds documentation entries
</verification>

<success_criteria>
- All tasks completed
- Dry-run produces real artifact files and summary
- Dry-run stays hardware/model independent
- Documentation explains the boundary honestly
</success_criteria>

<output>
After completion, create `.planning/phases/03-dry-run-adapters-and-reliability-hooks/03-02-SUMMARY.md`
</output>
