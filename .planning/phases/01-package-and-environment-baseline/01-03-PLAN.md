---
phase: 01-package-and-environment-baseline
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ENVIRONMENT.md
  - README.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06, CONF-01, CONF-02, CONF-03]
user_setup: []
must_haves:
  truths:
    - "Operator can read `docs/ENVIRONMENT.md` before a real experiment and find GPU server setup, robot-side setup, LAN checks, dry-run guidance, and common failures."
    - "Operator-facing constants are documented clearly enough to edit before running."
    - "README points operators to the environment guide."
  artifacts:
    - path: "docs/ENVIRONMENT.md"
      provides: "Environment setup guide"
      min_lines: 80
      contains: "## GPU Server Setup"
    - path: "README.md"
      provides: "Pointer to setup guide"
      contains: "docs/ENVIRONMENT.md"
  key_links:
    - from: "README.md"
      to: "docs/ENVIRONMENT.md"
      via: "documentation link"
      pattern: "docs/ENVIRONMENT\\.md"
---

<objective>
Write the Phase 1 environment setup guide and document the v1 constant-based configuration path.

Purpose: Make server, robot-side, LAN, dry-run, and troubleshooting setup explicit before real communication metrics are collected.
Output: `docs/ENVIRONMENT.md` plus README linkage to the guide.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-and-environment-baseline/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md

Relevant source files:
@README.md
@policy_server.py
@robot_client.py
</context>

<threat_model>
- T3-01: Environment guide overclaims support not implemented yet. Mitigation: mark dry-run and future config features according to current phase status.
- T3-02: Metrics become untrustworthy due to unsynchronized clocks. Mitigation: include time synchronization guidance for latency/RTT interpretation.
- T3-03: Operators expose the server unintentionally. Mitigation: document `HOST = "0.0.0.0"` LAN/firewall implications.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write GPU server setup section</name>
  <files>docs/ENVIRONMENT.md</files>
  <read_first>README.md, policy_server.py, .planning/phases/01-package-and-environment-baseline/01-RESEARCH.md</read_first>
  <action>Create `docs/ENVIRONMENT.md` if missing. Add `# Environment Setup` and a `## GPU Server Setup` section. Include subsections or bullet lists for Python environment, LeRobot installation, CUDA/PyTorch checks, SmolVLA model path or HuggingFace access, constants `HOST` and `PORT`, and policy server preflight. Include exact commands: `python3 -c "import torch; print(torch.cuda.is_available())"` and `python3 policy_server.py`.</action>
  <verify>rg "## GPU Server Setup|torch.cuda.is_available|python3 policy_server.py|HOST|PORT" docs/ENVIRONMENT.md</verify>
  <acceptance_criteria>
    - `docs/ENVIRONMENT.md` contains `## GPU Server Setup`
    - `docs/ENVIRONMENT.md` contains `torch.cuda.is_available`
    - `docs/ENVIRONMENT.md` contains `python3 policy_server.py`
    - `docs/ENVIRONMENT.md` contains `HOST`
    - `docs/ENVIRONMENT.md` contains `PORT`
  </acceptance_criteria>
  <done>GPU server setup is documented with concrete checks and constants.</done>
</task>

<task type="auto">
  <name>Task 2: Write robot-side setup section</name>
  <files>docs/ENVIRONMENT.md</files>
  <read_first>README.md, robot_client.py</read_first>
  <action>Add a `## Robot-Side Computer Setup` section. Cover LeRobot installation, SO-101 serial permissions, follower calibration id, camera detection, constants `SERVER_ADDRESS`, `ROBOT_PORT`, `ROBOT_ID`, `CAMERAS`, `TASK`, `POLICY_TYPE`, `PRETRAINED_NAME_OR_PATH`, `POLICY_DEVICE`, `ACTIONS_PER_CHUNK`, `CHUNK_SIZE_THRESHOLD`, `AGGREGATE_FN_NAME`, and `DEBUG_VISUALIZE_QUEUE_SIZE`. Include exact commands or examples: `ls /dev/ttyACM*`, `groups`, and `python3 robot_client.py`.</action>
  <verify>rg "## Robot-Side Computer Setup|SERVER_ADDRESS|ROBOT_PORT|ROBOT_ID|CAMERAS|PRETRAINED_NAME_OR_PATH|python3 robot_client.py" docs/ENVIRONMENT.md</verify>
  <acceptance_criteria>
    - `docs/ENVIRONMENT.md` contains `## Robot-Side Computer Setup`
    - `docs/ENVIRONMENT.md` contains `SERVER_ADDRESS`
    - `docs/ENVIRONMENT.md` contains `ROBOT_PORT`
    - `docs/ENVIRONMENT.md` contains `ROBOT_ID`
    - `docs/ENVIRONMENT.md` contains `PRETRAINED_NAME_OR_PATH`
    - `docs/ENVIRONMENT.md` contains `python3 robot_client.py`
  </acceptance_criteria>
  <done>Robot-side setup is documented with concrete checks and constants.</done>
</task>

<task type="auto">
  <name>Task 3: Write LAN, dry-run, and troubleshooting sections</name>
  <files>docs/ENVIRONMENT.md</files>
  <read_first>README.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Add sections named `## LAN Communication Checks`, `## Time Synchronization`, `## Dry-Run / Mock Setup`, and `## Common Failures`. Cover server IP/port, firewall, ping/basic connectivity, time synchronization guidance for trustworthy latency/RTT metrics, expected future dry-run usage, and common failures: LeRobot import failure, CUDA unavailable, invalid model path, camera index mismatch, serial port permission failure, server connection failure, and metrics output confusion.</action>
  <verify>rg "## LAN Communication Checks|## Time Synchronization|## Dry-Run / Mock Setup|## Common Failures|LeRobot import|CUDA unavailable|camera index|serial port|server connection|metrics" docs/ENVIRONMENT.md</verify>
  <acceptance_criteria>
    - `docs/ENVIRONMENT.md` contains `## LAN Communication Checks`
    - `docs/ENVIRONMENT.md` contains `## Time Synchronization`
    - `docs/ENVIRONMENT.md` contains `## Dry-Run / Mock Setup`
    - `docs/ENVIRONMENT.md` contains `## Common Failures`
    - `docs/ENVIRONMENT.md` contains `LeRobot import`
    - `docs/ENVIRONMENT.md` contains `CUDA unavailable`
    - `docs/ENVIRONMENT.md` contains `serial port`
  </acceptance_criteria>
  <done>LAN, dry-run, and troubleshooting guidance is documented.</done>
</task>

<task type="auto">
  <name>Task 4: Link environment guide from README</name>
  <files>README.md</files>
  <read_first>README.md, docs/ENVIRONMENT.md</read_first>
  <action>Add a short setup note near the README install/run sections that points to `docs/ENVIRONMENT.md` before real hardware experiments. Keep existing run commands `python3 policy_server.py`, `python3 robot_client.py`, and `python3 -m unittest discover -s tests -v` present.</action>
  <verify>rg "docs/ENVIRONMENT.md|python3 policy_server.py|python3 robot_client.py|python3 -m unittest discover -s tests -v" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `docs/ENVIRONMENT.md`
    - `README.md` contains `python3 policy_server.py`
    - `README.md` contains `python3 robot_client.py`
    - `README.md` contains `python3 -m unittest discover -s tests -v`
  </acceptance_criteria>
  <done>README directs operators to the environment guide without losing existing run instructions.</done>
</task>

</tasks>

<verification>
- [ ] `test -f docs/ENVIRONMENT.md` exits 0
- [ ] `rg "## GPU Server Setup|## Robot-Side Computer Setup|## LAN Communication Checks|## Time Synchronization|## Dry-Run / Mock Setup|## Common Failures" docs/ENVIRONMENT.md` finds all sections
- [ ] `rg "docs/ENVIRONMENT.md" README.md` finds a match
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Environment guide covers server, robot-side, LAN, dry-run, and troubleshooting setup
- README links to the environment guide
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-and-environment-baseline/01-03-SUMMARY.md`
</output>
