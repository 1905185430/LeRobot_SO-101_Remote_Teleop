---
phase: 01-package-and-environment-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - policy_server.py
  - robot_client.py
  - so101_remote/__init__.py
  - so101_remote/config.py
  - so101_remote/server.py
  - so101_remote/client.py
  - so101_remote/metrics.py
  - so101_remote/recorder.py
  - so101_remote/dryrun.py
  - so101_remote/adapters/__init__.py
  - so101_remote/adapters/robot.py
  - so101_remote/adapters/policy.py
  - so101_remote/adapters/lerobot_so101.py
autonomous: true
requirements: [STRC-01, STRC-02, STRC-03, CONF-01, CONF-02]
user_setup: []
must_haves:
  truths:
    - "Developer can import `so101_remote`, `so101_remote.server`, and `so101_remote.client` without LeRobot installed."
    - "Operator can still run `python3 policy_server.py` as the server entrypoint."
    - "Operator can still run `python3 robot_client.py` as the robot client entrypoint."
    - "Script-level constants remain the primary v1 configuration path."
  artifacts:
    - path: "so101_remote/__init__.py"
      provides: "Package import root"
      contains: "__all__"
    - path: "so101_remote/server.py"
      provides: "Policy server runtime helpers"
      contains: "def build_server_config"
    - path: "so101_remote/client.py"
      provides: "Robot client runtime helpers"
      contains: "def build_client_config"
    - path: "policy_server.py"
      provides: "Thin server entrypoint"
      contains: "from so101_remote"
    - path: "robot_client.py"
      provides: "Thin client entrypoint"
      contains: "from so101_remote"
  key_links:
    - from: "policy_server.py"
      to: "so101_remote.server"
      via: "imported build_server_config/main wrapper"
      pattern: "so101_remote\\.server"
    - from: "robot_client.py"
      to: "so101_remote.client"
      via: "imported client config/main wrapper"
      pattern: "so101_remote\\.client"
---

<objective>
Create the lightweight `so101_remote/` package skeleton and move runtime helper ownership behind thin top-level entrypoints.

Purpose: Establish the package shape required by Phase 1 without changing the intended LeRobot async inference behavior.
Output: Importable package modules, adapter placeholder modules, and still-runnable `policy_server.py` / `robot_client.py` entrypoints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-and-environment-baseline/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

Relevant source files:
@policy_server.py
@robot_client.py
</context>

<threat_model>
- T1-01: Import-time LeRobot dependency breaks local tests. Mitigation: keep LeRobot imports inside `_load_*_api()` functions and verify package imports without LeRobot.
- T1-02: Thin entrypoint loses existing operator constants. Mitigation: preserve current constant names in top-level scripts or re-export them exactly from `so101_remote.config`.
- T1-03: Runtime behavior changes before metrics phases exist. Mitigation: move helper functions with the same signatures first; do not redesign transport in this phase.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create lightweight package module skeleton</name>
  <files>so101_remote/__init__.py, so101_remote/config.py, so101_remote/server.py, so101_remote/client.py, so101_remote/metrics.py, so101_remote/recorder.py, so101_remote/dryrun.py, so101_remote/adapters/__init__.py, so101_remote/adapters/robot.py, so101_remote/adapters/policy.py, so101_remote/adapters/lerobot_so101.py</files>
  <read_first>policy_server.py, robot_client.py, .planning/phases/01-package-and-environment-baseline/01-RESEARCH.md</read_first>
  <action>Create the `so101_remote/` package and `so101_remote/adapters/` subpackage. Add `__all__` to `so101_remote/__init__.py` listing `"client"`, `"config"`, `"server"`, `"metrics"`, `"recorder"`, and `"dryrun"`. In `config.py`, define dataclasses or simple constants that mirror existing operator-facing values: `HOST`, `PORT`, `SERVER_ADDRESS`, `ROBOT_PORT`, `ROBOT_ID`, `CAMERAS`, `TASK`, `POLICY_TYPE`, `PRETRAINED_NAME_OR_PATH`, `POLICY_DEVICE`, `ACTIONS_PER_CHUNK`, `CHUNK_SIZE_THRESHOLD`, `AGGREGATE_FN_NAME`, and `DEBUG_VISUALIZE_QUEUE_SIZE`. Add placeholder modules `metrics.py`, `recorder.py`, and `dryrun.py` with import-safe docstrings and no LeRobot imports. Add adapter placeholder protocols/classes in `adapters/robot.py`, `adapters/policy.py`, and `adapters/lerobot_so101.py` without requiring LeRobot at import time.</action>
  <verify>python3 -c "import so101_remote; import so101_remote.config; import so101_remote.metrics; import so101_remote.recorder; import so101_remote.dryrun; import so101_remote.adapters.robot; import so101_remote.adapters.policy; import so101_remote.adapters.lerobot_so101"</verify>
  <acceptance_criteria>
    - `so101_remote/__init__.py` contains `__all__`
    - `so101_remote/config.py` contains `SERVER_ADDRESS`
    - `so101_remote/config.py` contains `PRETRAINED_NAME_OR_PATH`
    - `so101_remote/adapters/robot.py` contains `RobotAdapter`
    - `so101_remote/adapters/policy.py` contains `PolicyAdapter`
    - `so101_remote/adapters/lerobot_so101.py` contains `SO101`
  </acceptance_criteria>
  <done>Package modules exist and import without LeRobot installed.</done>
</task>

<task type="auto">
  <name>Task 2: Move server helper ownership into package</name>
  <files>so101_remote/server.py, policy_server.py</files>
  <read_first>policy_server.py, tests/test_minimal_async_scripts.py</read_first>
  <action>Move the current server helper behavior into `so101_remote.server`: define `HOST`, `PORT`, `build_server_config()`, `main()`, and `_load_server_api()` with the same semantics as the current `policy_server.py`. Keep `_load_server_api()` importing `PolicyServerConfig` from `lerobot.async_inference.configs` and `serve` from `lerobot.async_inference.policy_server` inside the function. Replace `policy_server.py` with a thin compatibility wrapper that imports `HOST`, `PORT`, `build_server_config`, and `main` from `so101_remote.server`, and keeps `if __name__ == "__main__": raise SystemExit(main())`.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts.MinimalAsyncScriptTests.test_policy_server_builds_official_config tests.test_minimal_async_scripts.MinimalAsyncScriptTests.test_policy_server_main_calls_serve -v</verify>
  <acceptance_criteria>
    - `so101_remote/server.py` contains `def build_server_config`
    - `so101_remote/server.py` contains `def _load_server_api`
    - `policy_server.py` contains `from so101_remote.server import`
    - `policy_server.py` contains `raise SystemExit(main())`
  </acceptance_criteria>
  <done>Server entrypoint behavior is owned by `so101_remote.server` and the top-level script remains compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Move client helper ownership into package</name>
  <files>so101_remote/client.py, robot_client.py</files>
  <read_first>robot_client.py, tests/test_minimal_async_scripts.py</read_first>
  <action>Move the current client helper behavior into `so101_remote.client`: define all current constants, `build_camera_configs()`, `build_robot_config()`, `build_client_config()`, `main()`, `_load_client_api()`, and `_load_so101_follower_config()` with the same semantics as the current `robot_client.py`. Keep all LeRobot imports lazy inside `_load_client_api()` and `_load_so101_follower_config()`. Replace `robot_client.py` with a thin compatibility wrapper that imports all operator-facing constants plus `build_camera_configs`, `build_robot_config`, `build_client_config`, and `main` from `so101_remote.client`, and keeps `if __name__ == "__main__": raise SystemExit(main())`.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts.MinimalAsyncScriptTests.test_robot_client_builds_expected_config tests.test_minimal_async_scripts.MinimalAsyncScriptTests.test_robot_client_main_starts_and_runs_control_loop -v</verify>
  <acceptance_criteria>
    - `so101_remote/client.py` contains `def build_client_config`
    - `so101_remote/client.py` contains `def build_robot_config`
    - `so101_remote/client.py` contains `def build_camera_configs`
    - `so101_remote/client.py` contains `def _load_so101_follower_config`
    - `robot_client.py` contains `from so101_remote.client import`
    - `robot_client.py` contains `raise SystemExit(main())`
  </acceptance_criteria>
  <done>Client entrypoint behavior is owned by `so101_remote.client` and the top-level script remains compatible.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -c "import so101_remote; import so101_remote.server; import so101_remote.client"` exits 0
- [ ] `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
- [ ] `rg "from so101_remote.server import" policy_server.py` finds a match
- [ ] `rg "from so101_remote.client import" robot_client.py` finds a match
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Package import root and runtime modules exist
- Top-level entrypoints remain runnable wrappers
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-and-environment-baseline/01-01-SUMMARY.md`
</output>
