---
phase: 01-package-and-environment-baseline
plan: "02"
type: execute
wave: 2
depends_on: ["01-01", "01-03"]
files_modified:
  - tests/test_minimal_async_scripts.py
  - tests/test_legacy_demo.py
  - legacy/__init__.py
  - legacy/tests/__init__.py
  - README.md
autonomous: true
requirements: [STRC-04]
user_setup: []
must_haves:
  truths:
    - "Existing legacy teleoperation tests still run from top-level unittest discovery."
    - "Current minimal async script tests still pass after entrypoint wrappers delegate into `so101_remote`."
    - "The new main runtime path does not import `legacy`."
  artifacts:
    - path: "tests/test_legacy_demo.py"
      provides: "Top-level legacy test bridge"
      contains: "legacy.tests"
    - path: "legacy/__init__.py"
      provides: "Legacy package boundary"
      min_lines: 1
    - path: "tests/test_minimal_async_scripts.py"
      provides: "Compatibility tests for thin entrypoints"
      contains: "import policy_server"
  key_links:
    - from: "tests/test_legacy_demo.py"
      to: "legacy.tests"
      via: "imported test classes"
      pattern: "from legacy\\.tests"
---

<objective>
Protect legacy teleoperation compatibility and update tests around the new package handoff.

Purpose: Ensure Phase 1 restructure does not break retained UDP teleoperation reference code or current async script behavior.
Output: Passing tests that prove legacy remains isolated and top-level entrypoints remain compatible.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-package-and-environment-baseline/01-RESEARCH.md
@.planning/phases/01-package-and-environment-baseline/01-01-SUMMARY.md
@.planning/phases/01-package-and-environment-baseline/01-03-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/STRUCTURE.md

Relevant source files:
@tests/test_minimal_async_scripts.py
@tests/test_legacy_demo.py
@legacy/__init__.py
@legacy/tests/__init__.py
</context>

<threat_model>
- T2-01: Package restructure breaks top-level imports expected by tests. Mitigation: keep compatibility imports in `policy_server.py` and `robot_client.py`, and update tests only where necessary.
- T2-02: Legacy path accidentally becomes coupled to the new runtime. Mitigation: assert or inspect that `so101_remote` modules do not import `legacy`.
- T2-03: Test discovery misses legacy tests. Mitigation: preserve `tests/test_legacy_demo.py` bridge and run `python3 -m unittest discover -s tests -v`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Update minimal async tests for thin wrappers</name>
  <files>tests/test_minimal_async_scripts.py</files>
  <read_first>tests/test_minimal_async_scripts.py, policy_server.py, robot_client.py, so101_remote/server.py, so101_remote/client.py</read_first>
  <action>Update `tests/test_minimal_async_scripts.py` only if needed after Plan 01. Keep imports of top-level `policy_server` and `robot_client` so wrapper compatibility is tested. Ensure fake LeRobot modules still patch `sys.modules`. Add assertions that `policy_server.build_server_config` and `robot_client.build_client_config` remain callable from the top-level modules.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts -v</verify>
  <acceptance_criteria>
    - `tests/test_minimal_async_scripts.py` contains `import policy_server`
    - `tests/test_minimal_async_scripts.py` contains `import robot_client`
    - `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
  </acceptance_criteria>
  <done>Current entrypoint compatibility tests pass after the package handoff.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve legacy test discovery bridge</name>
  <files>tests/test_legacy_demo.py, legacy/__init__.py, legacy/tests/__init__.py</files>
  <read_first>tests/test_legacy_demo.py, legacy/tests/test_protocol.py, legacy/tests/test_runtime.py</read_first>
  <action>Keep `tests/test_legacy_demo.py` importing `ProtocolTests`, `FollowerReceiverTests`, and `LeaderSenderTests` from `legacy.tests`. Ensure `legacy/__init__.py` and `legacy/tests/__init__.py` remain present so `legacy.tests` imports work. Do not move legacy modules into `so101_remote` during this phase.</action>
  <verify>python3 -m unittest tests.test_legacy_demo -v</verify>
  <acceptance_criteria>
    - `tests/test_legacy_demo.py` contains `from legacy.tests.test_protocol import ProtocolTests`
    - `tests/test_legacy_demo.py` contains `from legacy.tests.test_runtime import FollowerReceiverTests, LeaderSenderTests`
    - `python3 -m unittest tests.test_legacy_demo -v` exits 0
  </acceptance_criteria>
  <done>Legacy tests remain reachable from top-level discovery.</done>
</task>

<task type="auto">
  <name>Task 3: Verify main runtime does not depend on legacy</name>
  <files>README.md</files>
  <read_first>README.md, docs/ENVIRONMENT.md, so101_remote/server.py, so101_remote/client.py, so101_remote/adapters/lerobot_so101.py</read_first>
  <action>Do not add `legacy` imports to `so101_remote/server.py`, `so101_remote/client.py`, or `so101_remote/adapters/lerobot_so101.py`. If README wording changes, keep the statement that `legacy/` is retained as the old custom UDP teleop reference and is not the recommended inference path.</action>
  <verify>! rg "import legacy|from legacy" so101_remote policy_server.py robot_client.py</verify>
  <acceptance_criteria>
    - `rg "old custom UDP teleop reference|legacy/" README.md` finds a match
    - `rg "import legacy|from legacy" so101_remote policy_server.py robot_client.py` exits 1
  </acceptance_criteria>
  <done>Legacy remains isolated from the new main runtime path.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
- [ ] `python3 -m unittest tests.test_legacy_demo -v` exits 0
- [ ] `python3 -m unittest discover -s tests -v` exits 0
- [ ] `rg "import legacy|from legacy" so101_remote policy_server.py robot_client.py` exits 1
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Legacy test bridge remains intact
- New runtime package remains decoupled from legacy
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-and-environment-baseline/01-02-SUMMARY.md`
</output>
