---
phase: 05-validation-and-compatibility-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_minimal_async_scripts.py
  - tests/test_legacy_demo.py
  - tests/test_dryrun.py
  - tests/test_metrics.py
  - tests/test_recorder.py
  - tests/test_reliability.py
  - tests/test_adapters.py
autonomous: true
requirements: [RELY-04, RELY-05]
must_haves:
  truths:
    - "Repository-level unittest discovery passes after the package restructure."
    - "Focused thin-entrypoint, package module, metrics, dry-run, recorder, reliability, adapter, and legacy compatibility tests pass."
    - "Legacy UDP teleoperation remains retained compatibility/reference code, not the v1 main runtime path."
    - "No new runtime capability is added while fixing test or compatibility issues."
    - "Covers CONTEXT decisions D-03, D-04, D-07, and D-08."
  artifacts:
    - path: "tests/test_legacy_demo.py"
      provides: "Top-level discovery bridge for retained legacy UDP teleoperation tests"
      contains: "FollowerReceiverTests"
    - path: "tests/test_minimal_async_scripts.py"
      provides: "Fake-LeRobot coverage for thin entrypoints and runtime artifact paths"
      contains: "MinimalAsyncScriptTests"
---

<objective>
Verify and harden the automated test suite that proves the restructured package, thin entrypoints, metrics, dry-run, reliability helpers, adapters, and retained legacy UDP teleoperation compatibility still work.

Purpose: Phase 5 should leave v1 with a repeatable automated confidence check before any hardware session, while keeping legacy teleoperation protected as compatibility/reference code only.
</objective>

<context>
@.planning/phases/05-validation-and-compatibility-hardening/05-CONTEXT.md
@.planning/phases/05-validation-and-compatibility-hardening/05-PATTERNS.md
@.planning/REQUIREMENTS.md
@tests/test_minimal_async_scripts.py
@tests/test_legacy_demo.py
@tests/test_dryrun.py
@tests/test_metrics.py
@tests/test_recorder.py
@tests/test_reliability.py
@tests/test_adapters.py
@legacy/tests/test_protocol.py
@legacy/tests/test_runtime.py
</context>

<threat_model>
- T5-01: Top-level discovery stops running legacy tests, creating a false compatibility signal. Mitigation: verify `tests/test_legacy_demo.py` still imports `ProtocolTests`, `FollowerReceiverTests`, and `LeaderSenderTests`, and run full discovery.
- T5-02: Automated tests pass by dropping coverage rather than preserving behavior. Mitigation: only make small fixes that keep or strengthen existing coverage; do not delete test classes or assertions to get green tests.
- T5-03: Test hardening accidentally adds new runtime capability. Mitigation: restrict edits to tests or compatibility glue required by existing behavior; no new transport, robot, policy, config, or telemetry capability in this plan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify focused package and runtime test modules</name>
  <files>tests/test_minimal_async_scripts.py, tests/test_dryrun.py, tests/test_metrics.py, tests/test_recorder.py, tests/test_reliability.py, tests/test_adapters.py</files>
  <read_first>tests/test_minimal_async_scripts.py, tests/test_dryrun.py, tests/test_metrics.py, tests/test_recorder.py, tests/test_reliability.py, tests/test_adapters.py, so101_remote/server.py, so101_remote/client.py, so101_remote/dryrun.py, so101_remote/metrics.py, so101_remote/recorder.py, so101_remote/reliability.py, so101_remote/adapters/robot.py, so101_remote/adapters/policy.py, so101_remote/adapters/lerobot_so101.py</read_first>
  <action>Run focused package/runtime verification commands: `python3 -m unittest tests.test_minimal_async_scripts -v`, `python3 -m unittest tests.test_dryrun tests.test_metrics tests.test_recorder tests.test_reliability tests.test_adapters -v`. If a failure appears, fix only the affected test expectation or existing compatibility glue needed to preserve current behavior. Do not add new runtime capability. Keep `tests/test_minimal_async_scripts.py` using fake LeRobot modules instead of requiring real LeRobot.</action>
  <verify>python3 -m unittest tests.test_minimal_async_scripts -v && python3 -m unittest tests.test_dryrun tests.test_metrics tests.test_recorder tests.test_reliability tests.test_adapters -v</verify>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
    - `python3 -m unittest tests.test_dryrun tests.test_metrics tests.test_recorder tests.test_reliability tests.test_adapters -v` exits 0
    - `tests/test_minimal_async_scripts.py` contains `install_fake_lerobot_modules`
    - `tests/test_minimal_async_scripts.py` contains `MinimalAsyncScriptTests`
  </acceptance_criteria>
  <done>Package/runtime automated tests are green without real LeRobot or hardware.</done>
</task>

<task type="auto">
  <name>Task 2: Verify retained legacy teleoperation compatibility</name>
  <files>tests/test_legacy_demo.py, legacy/tests/test_protocol.py, legacy/tests/test_runtime.py</files>
  <read_first>tests/test_legacy_demo.py, legacy/tests/test_protocol.py, legacy/tests/test_runtime.py, legacy/protocol.py, legacy/leader_sender.py, legacy/follower_receiver.py, legacy/logging_utils.py</read_first>
  <action>Run `python3 -m unittest tests.test_legacy_demo -v`, `python3 -m unittest legacy.tests.test_protocol legacy.tests.test_runtime -v`, and confirm `tests/test_legacy_demo.py` imports `ProtocolTests`, `FollowerReceiverTests`, and `LeaderSenderTests`. If legacy tests fail from harmless import or compatibility drift, apply the smallest fix that preserves current legacy behavior. Do not migrate legacy UDP into `so101_remote` and do not add new wireless teleoperation features.</action>
  <verify>python3 -m unittest tests.test_legacy_demo -v && python3 -m unittest legacy.tests.test_protocol legacy.tests.test_runtime -v</verify>
  <acceptance_criteria>
    - `python3 -m unittest tests.test_legacy_demo -v` exits 0
    - `python3 -m unittest legacy.tests.test_protocol legacy.tests.test_runtime -v` exits 0
    - `tests/test_legacy_demo.py` contains `ProtocolTests`
    - `tests/test_legacy_demo.py` contains `FollowerReceiverTests`
    - `tests/test_legacy_demo.py` contains `LeaderSenderTests`
  </acceptance_criteria>
  <done>Legacy UDP teleoperation compatibility remains protected by top-level and direct legacy tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run repository-level verification</name>
  <files>tests/test_minimal_async_scripts.py, tests/test_legacy_demo.py, tests/test_dryrun.py, tests/test_metrics.py, tests/test_recorder.py, tests/test_reliability.py, tests/test_adapters.py</files>
  <read_first>tests/test_minimal_async_scripts.py, tests/test_legacy_demo.py, tests/test_dryrun.py, tests/test_metrics.py, tests/test_recorder.py, tests/test_reliability.py, tests/test_adapters.py</read_first>
  <action>Run `python3 -m unittest discover -s tests -v`. If discovery fails, fix only test discovery, test expectations, or compatibility glue required by current behavior. Leave unrelated local operator config changes alone, including any uncommitted `so101_remote/config.py` IP edits.</action>
  <verify>python3 -m unittest discover -s tests -v</verify>
  <acceptance_criteria>
    - `python3 -m unittest discover -s tests -v` exits 0
    - `python3 -m unittest discover -s tests -v` output includes `Ran 55 tests` or a higher count if legitimate tests were added
    - `git diff --check` exits 0
  </acceptance_criteria>
  <done>RELY-04 and RELY-05 have automated verification evidence.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_minimal_async_scripts -v` exits 0
- [ ] `python3 -m unittest tests.test_dryrun tests.test_metrics tests.test_recorder tests.test_reliability tests.test_adapters -v` exits 0
- [ ] `python3 -m unittest tests.test_legacy_demo -v` exits 0
- [ ] `python3 -m unittest legacy.tests.test_protocol legacy.tests.test_runtime -v` exits 0
- [ ] `python3 -m unittest discover -s tests -v` exits 0
- [ ] `rg "ProtocolTests|FollowerReceiverTests|LeaderSenderTests" tests/test_legacy_demo.py` finds all three names
</verification>

<success_criteria>
- Existing minimal async script tests pass after the restructure.
- Legacy UDP teleoperation tests pass from top-level discovery.
- RELY-04 and RELY-05 have repeatable automated evidence.
- No new runtime feature is introduced while hardening tests.
</success_criteria>

