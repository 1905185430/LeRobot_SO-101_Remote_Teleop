---
phase: 05-validation-and-compatibility-hardening
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/VALIDATION.md
  - docs/ENVIRONMENT.md
  - README.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md
autonomous: true
requirements: [RELY-04, RELY-05]
must_haves:
  truths:
    - "Validation notes explain unit-only, dry-run-only, real LeRobot required, hardware-required, and 10-30 min LAN required checks."
    - "D-02: Phase 5 preserves pending human validation where hardware or real LAN behavior is required and does not imply automated proof of SO-101 hardware, camera frames, model loading, or physical control-loop stability."
    - "Phase 5 documentation states that real LAN/hardware UAT remains pending unless actually performed."
    - "Legacy UDP teleoperation is documented as retained compatibility/reference code, not the v1 main runtime path."
    - "D-05: v2 continuation items are documented as separate directions: multi-arm support, wireless teleoperation integration, VLA/PI policy expansion, YAML/CLI configuration, non-LAN deployment, and reporting/plots."
    - "v2 continuation items are split into separate directions and do not block v1."
    - "D-06: The pending todo `Plan multi-arm wireless teleoperation and VLA inference` is folded into Phase 5 as documentation/planning context only and does not authorize implementation in Phase 5."
    - "The folded todo for multi-arm wireless teleoperation and VLA inference is reflected as future work, not implemented."
    - "D-09: Phase 5 can mark v1 code, documentation, and automated verification complete while leaving real 10-30 minute LAN/hardware UAT tracked as pending human validation."
    - "Covers CONTEXT decisions D-01 through D-10."
  artifacts:
    - path: "docs/VALIDATION.md"
      provides: "Strict layered validation matrix and v1 completion status"
      contains: "Validation Matrix"
    - path: "README.md"
      provides: "Operator-facing pointer to validation status and legacy positioning"
      contains: "Validation Status"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability status for RELY-04 and RELY-05"
      contains: "RELY-04"
---

<objective>
Document Phase 5 validation status, residual human hardware risk, retained legacy compatibility positioning, and the split v2 continuation path.

Purpose: v1 should close honestly: code/docs/automated readiness can be complete while real SO-101 + SmolVLA LAN validation remains tracked as human UAT.
</objective>

<context>
@.planning/phases/05-validation-and-compatibility-hardening/05-CONTEXT.md
@.planning/phases/05-validation-and-compatibility-hardening/05-PATTERNS.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-VERIFICATION.md
@.planning/phases/04-so-101-smolvla-runtime-integration/04-HUMAN-UAT.md
@README.md
@docs/ENVIRONMENT.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/todos/pending/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md
</context>

<threat_model>
- T5-04: Documentation overclaims hardware validation from unit tests. Mitigation: validation matrix must state hardware and LAN checks are pending human validation unless actually run.
- T5-05: v2 directions become accidental Phase 5 implementation scope. Mitigation: document split future items only; do not add runtime support for them.
- T5-06: Legacy teleoperation status becomes ambiguous. Mitigation: README and validation docs must say retained compatibility/reference code and not v1 main runtime.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add strict validation matrix documentation</name>
  <files>docs/VALIDATION.md, docs/ENVIRONMENT.md, README.md</files>
  <read_first>docs/ENVIRONMENT.md, README.md, .planning/phases/05-validation-and-compatibility-hardening/05-CONTEXT.md, .planning/phases/04-so-101-smolvla-runtime-integration/04-VERIFICATION.md, .planning/phases/04-so-101-smolvla-runtime-integration/04-HUMAN-UAT.md</read_first>
  <action>Create `docs/VALIDATION.md` with a `# Validation Status` title and a `## Validation Matrix` section. The matrix must include rows or bullets for exactly these validation layers: `unit-only`, `dry-run-only`, `real LeRobot required`, `hardware-required`, and `10-30 min LAN required`. Per D-02, state that `python3 -m unittest discover -s tests -v` covers automated unit/dry-run/legacy compatibility readiness but does not prove real SO-101 hardware, camera frames, SmolVLA model loading, physical control-loop stability, or LAN endurance. Per D-09, state that v1 code, documentation, and automated verification can be complete while real 10-30 minute LAN/hardware UAT remains pending human validation. Add a README section `## Validation Status` pointing to `docs/VALIDATION.md`. Add a short cross-reference in `docs/ENVIRONMENT.md` near the existing 10-30 minute LAN section.</action>
  <verify>rg "Validation Matrix|unit-only|dry-run-only|real LeRobot required|hardware-required|10-30 min LAN required" docs/VALIDATION.md && rg "Validation Status" README.md docs/ENVIRONMENT.md</verify>
  <acceptance_criteria>
    - `docs/VALIDATION.md` contains `# Validation Status`
    - `docs/VALIDATION.md` contains `## Validation Matrix`
    - `docs/VALIDATION.md` contains `unit-only`
    - `docs/VALIDATION.md` contains `dry-run-only`
    - `docs/VALIDATION.md` contains `real LeRobot required`
    - `docs/VALIDATION.md` contains `hardware-required`
    - `docs/VALIDATION.md` contains `10-30 min LAN required`
    - `README.md` contains `## Validation Status`
    - `docs/ENVIRONMENT.md` contains `docs/VALIDATION.md`
  </acceptance_criteria>
  <done>Operators can see exactly which validation layer proves which claim.</done>
</task>

<task type="auto">
  <name>Task 2: Document legacy status and split v2 continuation path</name>
  <files>docs/VALIDATION.md, README.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>docs/VALIDATION.md, README.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/05-validation-and-compatibility-hardening/05-CONTEXT.md</read_first>
  <action>Update `docs/VALIDATION.md` with `## Legacy Compatibility` stating that legacy UDP teleoperation is retained compatibility/reference code and not the v1 main runtime path. Update README's `## Legacy` section with the same status and keep the test command `python3 -m unittest discover -s tests -v`. Per D-05, add `## v2 Continuation Path` to `docs/VALIDATION.md` with separate bullets for `multi-arm support`, `wireless teleoperation integration`, `VLA/PI policy expansion`, `YAML/CLI configuration`, `non-LAN deployment`, and `reporting/plots`. Per D-06, represent the folded todo `Plan multi-arm wireless teleoperation and VLA inference` as future-work documentation only, not implementation authorization. Update `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md` only as documentation/tracing artifacts: keep v2 items deferred and mark RELY-04/RELY-05 as validated only if Plan 05-01 verification passed. Do not change runtime code.</action>
  <verify>rg "Legacy Compatibility|retained compatibility/reference code|not the v1 main runtime path|v2 Continuation Path|multi-arm support|wireless teleoperation integration|VLA/PI policy expansion|YAML/CLI configuration|non-LAN deployment|reporting/plots" docs/VALIDATION.md README.md .planning/PROJECT.md .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - `docs/VALIDATION.md` contains `## Legacy Compatibility`
    - `docs/VALIDATION.md` contains `retained compatibility/reference code`
    - `docs/VALIDATION.md` contains `## v2 Continuation Path`
    - `docs/VALIDATION.md` contains `multi-arm support`
    - `docs/VALIDATION.md` contains `wireless teleoperation integration`
    - `docs/VALIDATION.md` contains `VLA/PI policy expansion`
    - `docs/VALIDATION.md` contains `YAML/CLI configuration`
    - `docs/VALIDATION.md` contains `non-LAN deployment`
    - `docs/VALIDATION.md` contains `reporting/plots`
    - `README.md` contains `not the v1 main runtime path`
  </acceptance_criteria>
  <done>Future work is visible as split v2 directions without expanding Phase 5 scope.</done>
</task>

<task type="auto">
  <name>Task 3: Close folded todo in planning records if represented</name>
  <files>.planning/todos/pending/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, docs/VALIDATION.md</files>
  <read_first>.planning/todos/pending/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md, docs/VALIDATION.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>If `docs/VALIDATION.md` and planning documents clearly represent the folded todo's future-work intent, move `.planning/todos/pending/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md` to `.planning/todos/completed/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md` and add a short note in the moved file saying it was folded into Phase 5 v2 continuation documentation. If the docs do not yet represent it clearly, leave the todo pending and explain why in the plan summary. Do not delete the todo.</action>
  <verify>test -f .planning/todos/completed/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md || test -f .planning/todos/pending/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md</verify>
  <acceptance_criteria>
    - Either `.planning/todos/completed/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md` exists
    - Or `.planning/todos/pending/2026-05-11-plan-multi-arm-wireless-teleoperation-and-vla-inference.md` exists with the summary explaining why it stayed pending
    - No command deletes the todo without preserving its content
  </acceptance_criteria>
  <done>The folded todo is either closed as represented or intentionally kept pending.</done>
</task>

<task type="auto">
  <name>Task 4: Run final documentation and test verification</name>
  <files>docs/VALIDATION.md, docs/ENVIRONMENT.md, README.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>docs/VALIDATION.md, docs/ENVIRONMENT.md, README.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Run `python3 -m unittest discover -s tests -v`, `rg "Validation Matrix|Legacy Compatibility|v2 Continuation Path" docs/VALIDATION.md`, `rg "RELY-04|RELY-05" .planning/REQUIREMENTS.md`, and `git diff --check`. Fix only documentation or traceability issues surfaced by these checks.</action>
  <verify>python3 -m unittest discover -s tests -v && rg "Validation Matrix|Legacy Compatibility|v2 Continuation Path" docs/VALIDATION.md && rg "RELY-04|RELY-05" .planning/REQUIREMENTS.md && git diff --check</verify>
  <acceptance_criteria>
    - `python3 -m unittest discover -s tests -v` exits 0
    - `rg "Validation Matrix|Legacy Compatibility|v2 Continuation Path" docs/VALIDATION.md` exits 0
    - `rg "RELY-04|RELY-05" .planning/REQUIREMENTS.md` exits 0
    - `git diff --check` exits 0
  </acceptance_criteria>
  <done>Documentation closure remains backed by a passing automated suite.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest discover -s tests -v` exits 0
- [ ] `rg "Validation Matrix|unit-only|dry-run-only|real LeRobot required|hardware-required|10-30 min LAN required" docs/VALIDATION.md` exits 0
- [ ] `rg "Legacy Compatibility|retained compatibility/reference code|not the v1 main runtime path" docs/VALIDATION.md README.md` exits 0
- [ ] `rg "multi-arm support|wireless teleoperation integration|VLA/PI policy expansion|YAML/CLI configuration|non-LAN deployment|reporting/plots" docs/VALIDATION.md` exits 0
- [ ] `rg "RELY-04|RELY-05" .planning/REQUIREMENTS.md` exits 0
- [ ] `git diff --check` exits 0
</verification>

<success_criteria>
- Validation notes explain which checks are unit-only, dry-run-only, real LeRobot required, hardware-required, and LAN-required.
- Legacy UDP teleoperation remains documented and tested as retained compatibility/reference code.
- v2 items are visible as split future directions without blocking v1.
- v1 can close as code/docs/automated validation complete while real LAN hardware UAT remains pending.
</success_criteria>
