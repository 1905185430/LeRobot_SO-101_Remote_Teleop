---
phase: 7
plan: 07-03
type: implementation
wave: 3
depends_on:
  - 07-01
  - 07-02
status: completed
requirements:
  - DATASET-TCP-01
  - DATASET-TCP-02
  - DATASET-TCP-03
  - DATASET-TCP-04
  - DATASET-TCP-05
  - DATASET-TCP-06
  - DATASET-TCP-07
files_modified:
  - docs/reproduction/SO101_LOCAL_TCP_DATASET_REPLAY.md
  - docs/reproduction/REPRODUCTION.md
  - configs/README.md
  - .planning/phases/07-so101-local-tcp-dataset-replay-baseline/07-VERIFICATION.md
autonomous: true
---

# Plan 07-03: Operator Documentation And Final Validation

<objective>
Document the SO-101 local TCP dataset replay workflow, keep boundaries clear, and verify that the full Phase 7 implementation satisfies the locked requirements.
</objective>

<must_haves>
- Implement DATASET-TCP-07.
- Document D-01, D-02, D-18, D-19, and D-20 as operator-facing flow.
- State that dataset collection and HuggingFace download are operator-provided inputs.
- State that two-machine remote TCP replay is deferred.
- Do not claim real SO-101 hardware validation unless it was performed during execution.
</must_haves>

<tasks>

## Task 1: Write Local Dataset Replay Reproduction Guide

<read_first>
- `docs/reproduction/SO101_LOCAL_TCP_TELEOP.md`
- `docs/reproduction/SO101_WIRELESS_TCP_TELEOP.md`
- `docs/setup/ENVIRONMENT.md`
- `07-CONTEXT.md`
</read_first>

<action>
Add `docs/reproduction/SO101_LOCAL_TCP_DATASET_REPLAY.md`. Cover prerequisites, official LeRobot replay preflight, YAML dataset fields, local follower startup, dataset replay client startup, dry-run/fake validation, expected artifacts, safety failure behavior, and boundaries.
</action>

<acceptance_criteria>
- Guide shows the exact commands:
  - `python3 scripts/run_teleop_follower.py --config configs/replay/local_so101_tcp_dataset.yaml`
  - `python3 scripts/run_dataset_replay_client.py --config configs/replay/local_so101_tcp_dataset.yaml`
- Guide references official `python -m lerobot.replay` / `lerobot-replay` as preflight, not as this project's TCP implementation.
- Guide names `127.0.0.1` as the Phase 7 baseline and defers two-machine remote replay.
</acceptance_criteria>

## Task 2: Update Indexes And Config Documentation

<read_first>
- `docs/reproduction/REPRODUCTION.md`
- `configs/README.md`
- `configs/replay/local_so101_tcp_dataset.yaml`
</read_first>

<action>
Link the new replay guide from the reproduction index and document the new replay config category/fields in config docs.
</action>

<acceptance_criteria>
- Operators browsing reproduction docs can find local TCP teleop, wireless TCP teleop, and local TCP dataset replay distinctly.
- Config docs list `configs/replay/local_so101_tcp_dataset.yaml` and explain the `dataset:` fields at a high level.
</acceptance_criteria>

## Task 3: Final Phase Verification

<read_first>
- `.planning/phases/07-so101-local-tcp-dataset-replay-baseline/07-SPEC.md`
- `.planning/phases/07-so101-local-tcp-dataset-replay-baseline/07-CONTEXT.md`
- `tests/`
</read_first>

<action>
Run the full automated test suite and documented dry-run commands. Write `07-VERIFICATION.md` with requirement coverage, commands run, pass/fail status, and any hardware-only checks left for operator UAT.
</action>

<acceptance_criteria>
- `python3 -m unittest discover -s tests -v` passes.
- Dataset replay dry-run command passes.
- Verification file maps all DATASET-TCP requirements to implementation evidence.
- Remaining real hardware validation is explicitly marked pending unless actually performed.
</acceptance_criteria>

</tasks>

<verification>
- `python3 scripts/run_dataset_replay_client.py --config configs/replay/local_so101_tcp_dataset.yaml --dry-run`
- `python3 scripts/run_teleop_follower.py --config configs/replay/local_so101_tcp_dataset.yaml --dry-run`
- `python3 -m unittest discover -s tests -v`
- `git diff --check`
</verification>

<success_criteria>
- DATASET-TCP-07 is implemented and all Phase 7 requirements have written verification evidence.
- Operator can follow docs from dataset preflight to local TCP replay.
- The phase is ready for execution completion and later remote TCP replay planning.
</success_criteria>
