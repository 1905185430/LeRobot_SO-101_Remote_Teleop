---
phase: 02-metrics-and-run-artifacts
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - so101_remote/recorder.py
  - tests/test_recorder.py
autonomous: true
requirements: [METR-04, METR-09, EXP-01, EXP-02, EXP-03, EXP-04]
user_setup: []
must_haves:
  truths:
    - "Each experiment run creates a unique local directory under `logs/experiments` by default or a caller-provided root in tests."
    - "Each run writes `metadata.json`, `metrics.jsonl`, `events.jsonl`, and `metrics.csv`."
    - "Timeout, disconnect, retry, recovery, and exception events are stored alongside numeric metrics."
  artifacts:
    - path: "so101_remote/recorder.py"
      provides: "Run directory, metadata, JSONL/CSV recorder, and artifact file ownership"
      contains: "JsonlMetricsRecorder"
    - path: "tests/test_recorder.py"
      provides: "Filesystem tests for run artifacts"
      contains: "RecorderTests"
  key_links:
    - from: "so101_remote/recorder.py"
      to: "so101_remote.metrics"
      via: "imports MetricSample and MetricEvent"
      pattern: "from \\.metrics import"
---

<objective>
Implement local experiment run directories and structured recorder outputs.

Purpose: Make every run produce reproducible local artifacts before the real SO-101 SmolVLA runtime is wired into the package.
Output: `so101_remote.recorder` plus `tests/test_recorder.py` covering metadata, JSONL, CSV, and event files.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-metrics-and-run-artifacts/02-RESEARCH.md
@.planning/phases/02-metrics-and-run-artifacts/02-VALIDATION.md
@.planning/phases/02-metrics-and-run-artifacts/02-01-PLAN.md

Relevant source files:
@so101_remote/metrics.py
@so101_remote/recorder.py
@tests/test_metrics.py
</context>

<threat_model>
- T2-04: Recorder writes outside the intended local experiment root. Mitigation: accept a root path, create children below it, and test with `tempfile.TemporaryDirectory`.
- T2-05: Run directories collide during quick repeated tests. Mitigation: include timestamp plus a short random hex suffix.
- T2-06: Metadata accidentally requires unavailable git or hardware state. Mitigation: git commit lookup must be best-effort and metadata builder must accept plain dictionaries.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement run directory and metadata helpers</name>
  <files>so101_remote/recorder.py</files>
  <read_first>so101_remote/recorder.py, so101_remote/config.py, so101_remote/metrics.py, .planning/phases/02-metrics-and-run-artifacts/02-RESEARCH.md</read_first>
  <action>Replace the placeholder `so101_remote/recorder.py` with standard-library code. Define `DEFAULT_RUN_ROOT = Path("logs/experiments")`. Add `current_git_commit() -> str | None` that runs `git rev-parse --short HEAD` with `subprocess.run(..., capture_output=True, text=True, check=False)` and returns `None` on failure or nonzero status. Add `create_run_directory(root: str | Path = DEFAULT_RUN_ROOT, role: str = "run", now: datetime | None = None) -> Path` that creates a unique child directory named `{YYYYMMDD-HHMMSS}-{role}-{short_id}` where `short_id` is 8 hex characters from `uuid.uuid4().hex[:8]`. Add `build_run_metadata(...) -> dict[str, object]` accepting keyword-only fields `role`, `created_at`, `server`, `robot`, `policy`, `extra`, and `git_commit`; include keys exactly `role`, `created_at`, `server`, `robot`, `policy`, `extra`, and `git_commit` in the returned dict. Use ISO 8601 timestamps.</action>
  <verify>python3 -c "from tempfile import TemporaryDirectory; from so101_remote.recorder import create_run_directory; import pathlib; d=TemporaryDirectory(); p=create_run_directory(d.name, role='client'); print(p.exists(), p.name)"</verify>
  <acceptance_criteria>
    - `so101_remote/recorder.py` contains `DEFAULT_RUN_ROOT = Path("logs/experiments")`
    - `so101_remote/recorder.py` contains `def create_run_directory`
    - `so101_remote/recorder.py` contains `def build_run_metadata`
    - `so101_remote/recorder.py` contains `def current_git_commit`
  </acceptance_criteria>
  <done>Recorder can create unique run directories and reproducibility metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement JSONL and CSV recorder</name>
  <files>so101_remote/recorder.py</files>
  <read_first>so101_remote/recorder.py, so101_remote/metrics.py</read_first>
  <action>Add `JsonlMetricsRecorder` class initialized with `run_dir: str | Path`, `metadata: Mapping[str, object] | None = None`, and `write_csv: bool = True`. On initialization, create `run_dir`, write `metadata.json` with sorted keys and indent 2 when metadata is provided, create paths `metrics.jsonl`, `events.jsonl`, and `metrics.csv`. Implement `record_sample(sample: MetricSample)`, `record_event(event: MetricEvent)`, `record_samples(samples)`, `record_events(events)`, `close()`, `__enter__`, and `__exit__`. `record_sample` appends one JSON object per line to `metrics.jsonl` using `sample.to_dict()` and appends CSV rows with header `timestamp,name,value,unit,tags` where `tags` is JSON-encoded with sorted keys. `record_event` appends one JSON object per line to `events.jsonl` using `event.to_dict()`. Keep files line-buffered or explicitly flushed after writes so tests can read immediately.</action>
  <verify>python3 -m unittest tests.test_recorder -v</verify>
  <acceptance_criteria>
    - `so101_remote/recorder.py` contains `class JsonlMetricsRecorder`
    - `so101_remote/recorder.py` contains `metrics.jsonl`
    - `so101_remote/recorder.py` contains `events.jsonl`
    - `so101_remote/recorder.py` contains `metrics.csv`
  </acceptance_criteria>
  <done>Numeric metrics and events are persisted as structured local artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Add recorder filesystem tests</name>
  <files>tests/test_recorder.py</files>
  <read_first>so101_remote/recorder.py, so101_remote/metrics.py, tests/test_metrics.py</read_first>
  <action>Create `tests/test_recorder.py` using `unittest` and `tempfile.TemporaryDirectory`. Add tests that prove: `create_run_directory()` creates two distinct directories under the provided root; `build_run_metadata()` returns keys `role`, `created_at`, `server`, `robot`, `policy`, `extra`, and `git_commit`; `JsonlMetricsRecorder` writes `metadata.json`, `metrics.jsonl`, `events.jsonl`, and `metrics.csv`; `metrics.jsonl` contains `"name": "latency_ms"` after recording a latency sample; `events.jsonl` contains `"event_type": "timeout"` after recording a timeout event; `metrics.csv` starts with `timestamp,name,value,unit,tags`.</action>
  <verify>python3 -m unittest tests.test_recorder -v</verify>
  <acceptance_criteria>
    - `tests/test_recorder.py` contains `class RecorderTests`
    - `tests/test_recorder.py` contains `test_create_run_directory_is_unique`
    - `tests/test_recorder.py` contains `test_recorder_writes_metadata_metrics_events_and_csv`
    - `python3 -m unittest tests.test_recorder -v` exits 0
  </acceptance_criteria>
  <done>Run artifact filesystem behavior is protected by automated tests.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_recorder -v` exits 0
- [ ] `python3 -m unittest tests.test_metrics tests.test_recorder -v` exits 0
- [ ] `rg "metadata.json|metrics.jsonl|events.jsonl|metrics.csv" so101_remote/recorder.py` finds all artifact names
- [ ] `rg "logs/experiments" so101_remote/recorder.py` finds the default run root
</verification>

<success_criteria>
- All tasks completed
- Run directories are unique
- Metadata is structured and reproducible
- Numeric samples and events are stored in local structured files
</success_criteria>

<output>
After completion, create `.planning/phases/02-metrics-and-run-artifacts/02-02-SUMMARY.md`
</output>
