---
phase: 02-metrics-and-run-artifacts
plan: "03"
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - so101_remote/recorder.py
  - tests/test_recorder.py
  - README.md
  - docs/ENVIRONMENT.md
autonomous: true
requirements: [METR-08, METR-09, EXP-01, EXP-03, EXP-04, EXP-05]
user_setup: []
must_haves:
  truths:
    - "Each run can produce a lightweight Markdown summary with count, min, max, mean, and p95 where data exists."
    - "Markdown summary includes event counts for timeout, disconnect, retry, recovery, and exception events when present."
    - "README and environment docs point operators to the run artifact location and expected files."
  artifacts:
    - path: "so101_remote/recorder.py"
      provides: "Markdown summary generation from metrics/events/metadata"
      contains: "write_summary_markdown"
    - path: "README.md"
      provides: "Operator-facing artifact contract"
      contains: "logs/experiments"
    - path: "docs/ENVIRONMENT.md"
      provides: "Environment guide note for metrics artifacts"
      contains: "summary.md"
  key_links:
    - from: "so101_remote/recorder.py"
      to: "so101_remote.metrics"
      via: "uses compute_stats/count_events for Markdown summaries"
      pattern: "compute_stats|count_events"
---

<objective>
Implement lightweight Markdown run summaries and document the experiment artifact contract.

Purpose: Give operators a stable local artifact layout and readable summary for communication experiments before real runtime hooks are integrated.
Output: `summary.md` generation, tests, and concise README/environment documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-metrics-and-run-artifacts/02-RESEARCH.md
@.planning/phases/02-metrics-and-run-artifacts/02-VALIDATION.md
@.planning/phases/02-metrics-and-run-artifacts/02-01-PLAN.md
@.planning/phases/02-metrics-and-run-artifacts/02-02-PLAN.md

Relevant source files:
@so101_remote/metrics.py
@so101_remote/recorder.py
@tests/test_recorder.py
@README.md
@docs/ENVIRONMENT.md
</context>

<threat_model>
- T2-07: Markdown summary silently omits events, hiding communication failures. Mitigation: include an event counts section and test for `timeout`.
- T2-08: Summary generation depends on real hardware or LeRobot. Mitigation: generate from in-memory samples/events and/or recorded files using standard-library code only.
- T2-09: Operators cannot find artifact outputs. Mitigation: document `logs/experiments/<run_id>/` and required files in README and environment guide.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement Markdown summary generation</name>
  <files>so101_remote/recorder.py</files>
  <read_first>so101_remote/recorder.py, so101_remote/metrics.py, .planning/phases/02-metrics-and-run-artifacts/02-RESEARCH.md</read_first>
  <action>Add `write_summary_markdown(run_dir: str | Path, metadata: Mapping[str, object] | None, samples: Sequence[MetricSample], events: Sequence[MetricEvent]) -> Path` to `so101_remote.recorder`. The function must write `summary.md` in `run_dir` and return its `Path`. The Markdown must include `# Run Summary`, `## Metadata`, `## Metric Statistics`, `## Event Counts`, and `## Files`. In `## Metric Statistics`, group samples by name and include a table header exactly `| Metric | Unit | Count | Min | Max | Mean | P95 |`. In `## Event Counts`, include a table header exactly `| Event | Count |`. In `## Files`, list `metadata.json`, `metrics.jsonl`, `events.jsonl`, and `metrics.csv` when those files exist in the run directory.</action>
  <verify>python3 -m unittest tests.test_recorder -v</verify>
  <acceptance_criteria>
    - `so101_remote/recorder.py` contains `def write_summary_markdown`
    - `so101_remote/recorder.py` contains `# Run Summary`
    - `so101_remote/recorder.py` contains `| Metric | Unit | Count | Min | Max | Mean | P95 |`
    - `so101_remote/recorder.py` contains `| Event | Count |`
  </acceptance_criteria>
  <done>Run summaries can be generated from structured metric/event data.</done>
</task>

<task type="auto">
  <name>Task 2: Add summary tests and recorder convenience method</name>
  <files>so101_remote/recorder.py, tests/test_recorder.py</files>
  <read_first>so101_remote/recorder.py, tests/test_recorder.py</read_first>
  <action>Update `JsonlMetricsRecorder` to keep in-memory `samples` and `events` lists as samples/events are recorded. Add `write_summary(self) -> Path` that calls `write_summary_markdown(self.run_dir, self.metadata, self.samples, self.events)`. Extend `tests/test_recorder.py` with a test that records latency samples `[10, 20, 30]` in `ms`, records one `timeout` event, calls `write_summary()`, and asserts `summary.md` contains `# Run Summary`, `latency_ms`, `| Metric | Unit | Count | Min | Max | Mean | P95 |`, `timeout`, `metadata.json`, `metrics.jsonl`, and `events.jsonl`.</action>
  <verify>python3 -m unittest tests.test_recorder -v</verify>
  <acceptance_criteria>
    - `so101_remote/recorder.py` contains `def write_summary(self)`
    - `tests/test_recorder.py` contains `test_write_summary_markdown_contains_stats_events_and_files`
    - `tests/test_recorder.py` contains `summary.md`
    - `python3 -m unittest tests.test_recorder -v` exits 0
  </acceptance_criteria>
  <done>Summary generation is available through the recorder and covered by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Document run artifact layout</name>
  <files>README.md, docs/ENVIRONMENT.md</files>
  <read_first>README.md, docs/ENVIRONMENT.md, .planning/phases/02-metrics-and-run-artifacts/02-RESEARCH.md</read_first>
  <action>Add a concise `## Experiment Artifacts` section to `README.md` after the `Run` section. State that Phase 2 stores local runs under `logs/experiments/<run_id>/` by default and list `metadata.json`, `metrics.jsonl`, `events.jsonl`, `metrics.csv`, and `summary.md`. In `docs/ENVIRONMENT.md`, add a short metrics artifact check that tells operators to inspect `logs/experiments/<run_id>/summary.md` and confirm `metadata.json`, `metrics.jsonl`, and `events.jsonl` exist after a run or dry-run. Do not claim real LeRobot metrics hooks are wired until later runtime phases.</action>
  <verify>rg "Experiment Artifacts|logs/experiments|summary.md|metrics.jsonl|events.jsonl" README.md docs/ENVIRONMENT.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Experiment Artifacts`
    - `README.md` contains `logs/experiments/<run_id>/`
    - `README.md` contains `summary.md`
    - `docs/ENVIRONMENT.md` contains `logs/experiments/<run_id>/summary.md`
    - `docs/ENVIRONMENT.md` contains `metrics.jsonl`
    - `docs/ENVIRONMENT.md` contains `events.jsonl`
  </acceptance_criteria>
  <done>Operators can find and inspect run artifacts.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_metrics tests.test_recorder -v` exits 0
- [ ] `python3 -m unittest discover -s tests -v` exits 0
- [ ] `rg "def write_summary_markdown|def write_summary\\(self\\)" so101_remote/recorder.py` finds both summary APIs
- [ ] `rg "logs/experiments/<run_id>/|summary.md|metrics.jsonl|events.jsonl" README.md docs/ENVIRONMENT.md` finds documentation entries
</verification>

<success_criteria>
- All tasks completed
- `summary.md` generation includes metadata, metric statistics, event counts, and file list
- Documentation names the run artifact location and required files
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/02-metrics-and-run-artifacts/02-03-SUMMARY.md`
</output>
