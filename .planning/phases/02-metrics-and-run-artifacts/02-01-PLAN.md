---
phase: 02-metrics-and-run-artifacts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - so101_remote/metrics.py
  - tests/test_metrics.py
autonomous: true
requirements: [METR-01, METR-02, METR-03, METR-04, METR-05, METR-06, METR-07, METR-08]
user_setup: []
must_haves:
  truths:
    - "`so101_remote.metrics` is importable without LeRobot installed."
    - "Runtime code can record latency, RTT-like timing, jitter, loop interval, chunk interval, queue state, and event metrics as structured objects."
    - "Terminal summaries include count, min, max, mean, and p95 for numeric metrics."
  artifacts:
    - path: "so101_remote/metrics.py"
      provides: "Metric sample/event models, statistics helpers, collector, and terminal summary formatting"
      contains: "MetricSample"
    - path: "tests/test_metrics.py"
      provides: "Deterministic unit tests for metrics behavior"
      contains: "MetricTests"
  key_links:
    - from: "so101_remote/metrics.py"
      to: "so101_remote/recorder.py"
      via: "plain dataclass/dict objects consumed by recorder in Plan 02"
      pattern: "asdict|to_dict"
---

<objective>
Implement the import-safe metric model and statistics layer for communication measurements.

Purpose: Make latency, RTT-like timing, jitter, loop interval, chunk interval, queue state, and runtime events first-class structured values before they are attached to real LeRobot runtime hooks.
Output: `so101_remote.metrics` with dataclasses/helpers plus `tests/test_metrics.py`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-metrics-and-run-artifacts/02-RESEARCH.md
@.planning/phases/02-metrics-and-run-artifacts/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md

Relevant source files:
@so101_remote/metrics.py
@so101_remote/recorder.py
@tests/test_minimal_async_scripts.py
</context>

<threat_model>
- T2-01: Metric helpers accidentally import LeRobot and break local testing. Mitigation: use only standard-library modules in `so101_remote.metrics`.
- T2-02: Percentile/jitter math is nondeterministic or ambiguous. Mitigation: implement sorted nearest-rank p95 and absolute adjacent deltas with explicit unit tests.
- T2-03: Event names drift across runtime code. Mitigation: define stable event type constants or documented string values: `timeout`, `disconnect`, `retry`, `recovery`, and `exception`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement metric dataclasses and constants</name>
  <files>so101_remote/metrics.py</files>
  <read_first>so101_remote/metrics.py, .planning/phases/02-metrics-and-run-artifacts/02-RESEARCH.md</read_first>
  <action>Replace the placeholder `so101_remote/metrics.py` with an import-safe standard-library implementation. Add `MetricSample` dataclass with fields `name: str`, `value: float`, `unit: str`, `timestamp: float | None = None`, and `tags: dict[str, str] | None = None`. Add `MetricEvent` dataclass with fields `event_type: str`, `message: str`, `timestamp: float | None = None`, `severity: str = "info"`, and `details: dict[str, str] | None = None`. Add `MetricStats` dataclass with fields `count: int`, `min: float | None`, `max: float | None`, `mean: float | None`, and `p95: float | None`. Define metric name constants `LATENCY_MS = "latency_ms"`, `RTT_MS = "rtt_ms"`, `JITTER_MS = "jitter_ms"`, `LOOP_INTERVAL_MS = "loop_interval_ms"`, `CHUNK_INTERVAL_MS = "chunk_interval_ms"`, and `QUEUE_SIZE = "queue_size"`. Define event constants `EVENT_TIMEOUT = "timeout"`, `EVENT_DISCONNECT = "disconnect"`, `EVENT_RETRY = "retry"`, `EVENT_RECOVERY = "recovery"`, and `EVENT_EXCEPTION = "exception"`. Add `to_dict()` methods on samples/events that omit no fields and normalize `tags`/`details` to `{}` when absent.</action>
  <verify>python3 -c "from so101_remote.metrics import MetricSample, MetricEvent, LATENCY_MS, EVENT_TIMEOUT; print(MetricSample(LATENCY_MS, 1.0, 'ms').to_dict()['name']); print(MetricEvent(EVENT_TIMEOUT, 'x').to_dict()['event_type'])"</verify>
  <acceptance_criteria>
    - `so101_remote/metrics.py` contains `class MetricSample`
    - `so101_remote/metrics.py` contains `class MetricEvent`
    - `so101_remote/metrics.py` contains `LATENCY_MS = "latency_ms"`
    - `so101_remote/metrics.py` contains `EVENT_EXCEPTION = "exception"`
  </acceptance_criteria>
  <done>Metric values and events have stable import-safe representations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement statistics, jitter, event counts, and collector</name>
  <files>so101_remote/metrics.py</files>
  <read_first>so101_remote/metrics.py</read_first>
  <action>Add `compute_stats(values: Iterable[float]) -> MetricStats` using count, min, max, arithmetic mean, and nearest-rank p95 where index is `ceil(0.95 * count) - 1` clamped into range. Add `derive_jitter_samples(samples: Sequence[MetricSample], name: str = JITTER_MS) -> list[MetricSample]` that sorts samples by `timestamp` when timestamps are present, computes absolute adjacent `value` deltas, uses the input unit, and timestamps each jitter sample with the later adjacent sample timestamp. Add `count_events(events: Iterable[MetricEvent]) -> dict[str, int]`. Add `MetricCollector` with methods `record_sample(name, value, unit, timestamp=None, tags=None)`, `record_event(event_type, message, timestamp=None, severity="info", details=None)`, `extend_samples(samples)`, `stats_by_name()`, and `event_counts()`. Keep internal lists accessible as `samples` and `events`.</action>
  <verify>python3 -m unittest tests.test_metrics -v</verify>
  <acceptance_criteria>
    - `so101_remote/metrics.py` contains `def compute_stats`
    - `so101_remote/metrics.py` contains `def derive_jitter_samples`
    - `so101_remote/metrics.py` contains `class MetricCollector`
    - `so101_remote/metrics.py` contains `def event_counts`
  </acceptance_criteria>
  <done>Metrics can be collected and summarized without filesystem or runtime dependencies.</done>
</task>

<task type="auto">
  <name>Task 3: Implement terminal summary formatting and tests</name>
  <files>so101_remote/metrics.py, tests/test_metrics.py</files>
  <read_first>so101_remote/metrics.py, tests/test_minimal_async_scripts.py</read_first>
  <action>Add `format_terminal_summary(stats_by_name: Mapping[str, MetricStats], event_counts: Mapping[str, int] | None = None) -> str`. The string must start with `Metrics summary`, include one line per metric in sorted metric-name order with exact labels `count=`, `min=`, `max=`, `mean=`, and `p95=`, and include `Events:` followed by sorted `event_type=count` pairs when events exist. Create `tests/test_metrics.py` using `unittest`. Include tests that prove: `MetricSample.to_dict()` preserves `name`, `value`, `unit`, and `tags`; `MetricEvent.to_dict()` preserves `event_type`, `severity`, and `details`; `compute_stats([1, 2, 3, 4, 100]).p95 == 100`; jitter from latency values `[10, 15, 12]` produces `[5, 3]`; `MetricCollector.event_counts()` counts `timeout` and `recovery`; terminal summary contains `Metrics summary`, `latency_ms`, `count=`, `mean=`, `p95=`, and `timeout=1`.</action>
  <verify>python3 -m unittest tests.test_metrics -v</verify>
  <acceptance_criteria>
    - `tests/test_metrics.py` contains `class MetricTests`
    - `tests/test_metrics.py` contains `test_compute_stats_uses_nearest_rank_p95`
    - `tests/test_metrics.py` contains `test_derive_jitter_samples`
    - `tests/test_metrics.py` contains `test_terminal_summary_includes_metric_stats_and_events`
    - `python3 -m unittest tests.test_metrics -v` exits 0
  </acceptance_criteria>
  <done>Metric model behavior is protected by focused unit tests.</done>
</task>

</tasks>

<verification>
- [ ] `python3 -m unittest tests.test_metrics -v` exits 0
- [ ] `python3 -c "import so101_remote.metrics"` exits 0
- [ ] `rg "latency_ms|rtt_ms|jitter_ms|loop_interval_ms|chunk_interval_ms|queue_size" so101_remote/metrics.py` finds all metric names
- [ ] `rg "timeout|disconnect|retry|recovery|exception" so101_remote/metrics.py` finds all event types
</verification>

<success_criteria>
- All tasks completed
- Metric helpers are pure standard-library code
- Numeric stats and event counts are deterministic
- Terminal summary output is covered by tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-metrics-and-run-artifacts/02-01-SUMMARY.md`
</output>
